# Project: Sebooth Website (Web Portal)

## Purpose

A web portal designed for Sebooth photobooth users to access, view, and download their digital assets (softfiles) via QR code redirection from the physical photobooth stations. Also serves as the company's public-facing website with product information, pricing, gallery, partnership program, and an admin CMS panel to manage all website content.

## AI Agent Rules

- **CRITICAL**: You (the AI Agent) MUST ALWAYS update this `agents.md` file whenever there is a new user prompt, a new feature built, or any significant code changes. This is to guarantee that `agents.md` is always up-to-date and serves as the absolute single source of truth for the project context.

## Technical Stack

- **Framework**: Next.js 16 (App Router)
- **UI/Logic**: React 19
- **Styling**: Tailwind CSS 4
- **Animations**: Framer Motion
- **Icons**: Lucide React
- **Language**: TypeScript
- **Backend/Database**: Supabase (Auth, Database, Storage)
- **Media Storage**: Google Cloud Storage (photos, videos, GIFs)
- **Auth Library**: `@supabase/ssr` (cookie-based SSR auth)
- **Caching**: ISR (Incremental Static Regeneration, 60s revalidation)
- **Utilities**: `clsx`, `tailwind-merge`

## Directory Structure

```
sebooth-website/
├── public/                          # Static assets
│   ├── frames/                      # SVG frame overlay templates for strip editor
│   ├── images/                      # Image assets (paper-noise.png, main-background.png)
│   ├── logo-text-black.png          # Brand logo
│   └── *.svg                        # Default Next.js icons
├── src/
│   ├── app/                         # App Router pages and layouts
│   │   ├── layout.tsx               # Root layout (Inter + custom Sebooth font)
│   │   ├── globals.css              # Global CSS (Tailwind 4 imports)
│   │   ├── page.tsx                 # Homepage (ISR, server-side data fetching)
│   │   ├── actions.ts               # Server Actions (revalidateSiteContent, revalidateSpecificPage)
│   │   ├── about/page.tsx           # About Us page (ISR Server Component + CMS + SEO)
│   │   ├── access/[id]/page.tsx     # QR Access Point (Server Component + generateMetadata)
│   │   ├── admin/page.tsx           # Admin CMS Panel (content, pricing, IG, news, admins)
│   │   ├── login/page.tsx           # Login page (email/password + claim flow)
│   │   ├── partnership/page.tsx     # Partnership page (ISR Server Component + CMS + SEO)
│   │   ├── profile/page.tsx         # User gallery (Feed + Gallery dual-mode view)
│   │   ├── profile/[sessionId]/page.tsx # Session detail (tab-based strip/GIF/live/photos view)
│   │   └── register/page.tsx        # Registration page (signup + session claim)
│   ├── components/
│   │   ├── features/
│   │   │   ├── FrameEditorModal.tsx  # Canvas-based strip frame editor modal
│   │   │   └── AccessSessionClient.tsx # Client component for /access/[id] (claim UI + auth)
│   │   ├── layout/
│   │   │   ├── Header.tsx            # Site header (responsive nav + mobile hamburger)
│   │   │   ├── Footer.tsx            # Site footer (4-column: brand, nav, social, office)
│   │   │   └── LayoutShell.tsx       # Conditional layout (excludes header/footer on auth/app pages)
│   │   ├── sections/                 # Homepage section components (all receive server-side data via props)
│   │   │   ├── Hero.tsx              # Hero banner (cinematic fullscreen + CTA)
│   │   │   ├── About.tsx             # About teaser section (links to /about)
│   │   │   ├── AboutContent.tsx      # Client component for /about page (CMS-driven, Framer Motion)
│   │   │   ├── Product.tsx           # Product showcase (Standard, Deluxe, Glamour booths)
│   │   │   ├── Pricing.tsx           # Pricing tables (Unlimited + Quota packages)
│   │   │   ├── Testimonials.tsx      # Customer testimonials (3-column grid)
│   │   │   ├── Gallery.tsx           # Event gallery (server-fetched, next/image, masonry grid)
│   │   │   ├── InstagramFeed.tsx     # Instagram feed (server-fetched posts, client-side embed script)
│   │   │   ├── FAQ.tsx               # FAQ accordion (animated expand/collapse)
│   │   │   ├── News.tsx              # Latest news grid (server-fetched, next/image)
│   │   │   ├── PartnershipContent.tsx # Client component for /partnership page (CMS-driven)
│   │   │   └── Location.tsx          # Location/map section (studio address)
│   │   └── ui/
│   │       └── FloatingCTA.tsx       # Floating WhatsApp button
│   ├── lib/
│   │   ├── supabase.ts              # Supabase browser client factory
│   │   ├── serverSupabase.ts        # Server-side Supabase client + ISR data fetchers
│   │   ├── useSiteContent.ts        # parseJsonContent() utility for CMS JSON fields
│   │   ├── useSectionVisibility.ts  # Client-side section visibility hook (fallback)
│   │   └── utils.ts                 # cn() utility (clsx + tailwind-merge)
│   ├── types/
│   │   └── database.ts              # TypeScript interfaces (SessionData, MediaItem)
│   └── middleware.ts                # Auth middleware (optimized: skips public routes)
├── .env.local                       # Environment variables (Supabase URL/keys, admin emails)
├── package.json
├── tsconfig.json
├── next.config.ts
├── postcss.config.mjs
└── eslint.config.mjs
```

## Design System

- **Color Palette**:
  - Primary Dark: `#1A1A1A` (text & backgrounds)
  - Primary Green: `#0F3D2E` (brand accent, CTAs)
  - Gold Accent: `#D4AF37` (premium highlights)
  - Light Background: `#F9F9F9`
  - Neutral Gray: `#E5E5E5` / `#EAEAEA`
- **Typography**:
  - All fonts (body, headline, brand, marker): Poppins (Google Font, `--font-poppins` mapped globally to override headline, marker, and custom fonts)

## Supabase Database Tables

| Table              | Purpose                                           |
|--------------------|---------------------------------------------------|
| `sessions`         | Photobooth session records (event name, user link, claim status) |
| `media`            | Media files linked to sessions (photos, GIFs, videos, strips) |
| `site_content`     | CMS key-value content for homepage sections       |
| `instagram_posts`  | Instagram post URLs for embed feed section         |
| `news`             | News/announcements managed via admin panel         |
| `admins`           | Admin user registry (email + super admin flag)     |

## Authentication & Authorization

- **Provider**: Supabase Auth (email/password)
- **Middleware**: SSR cookie-based auth via `@supabase/ssr`; protects `/profile` and `/admin` routes (redirects unauthenticated users to `/login`)
- **Admin Access**: Dual validation — environment variable (`NEXT_PUBLIC_ADMIN_EMAILS`) + `admins` DB table; super admins can invite other admins
- **Session Claiming**: QR scan → `/access/[id]` → auto-claim (if logged in) or redirect to `/register?claim=[id]` or `/login?claim=[id]`

## Key Features

1. **Landing Page (Homepage)**: Full company website with 10 sections — Hero, About, Product, Pricing, Testimonials, Gallery, Instagram Feed, FAQ, News, Location. All data fetched server-side with ISR (60s revalidation).
2. **QR Access Point** (`/access/[id]`): Direct link from QR code at photobooth; shows session preview, handles auto-claim for logged-in users, and prompts registration/login for anonymous users.
3. **User Authentication** (`/login`, `/register`): Email/password auth with integrated session claiming flow (via `?claim=[id]` query parameter).
4. **User Gallery** (`/profile`): Dual-mode photo gallery — **Feed View** (vertical scroll of all sessions as Instagram-style cards with strip thumbnails, media indicators, and "See More" CTA) and **Gallery View** (2-column grid of all photo strips). Toggle via grid icon in nav bar.
5. **Session Detail** (`/profile/[sessionId]`): Single-session deep view with 4 tabs — Photo Strip (with frame template generator), GIF, Live Photo (video), and individual Photos; all with download functionality.
5. **Photo Strip Generator**: Client-side canvas rendering of photo strips with multiple frame templates (Classic White, Night Edition, Wedding Elegant); carousel navigation between templates.
6. **Frame Editor Modal**: Advanced canvas-based frame compositor modal for applying SVG frame overlays to raw photos.
7. **About Page** (`/about`): Company narrative (origin story, vision & mission with numbered steps).
8. **Partnership Page** (`/partnership`): B2B page for Event Organizers & Wedding Organizers with benefits overview and inquiry form.
9. **Admin Panel** (`/admin`): Media/gallery frame asset management, live queue management (`QueueOperatorTab`), news publishing, and admin access control.
10. **Modern Slide Deck Presentation Engine** (`SlideDeckLanding.tsx`): High-performance 6-slide full-screen presentation system (Hero, Services, Frames, Portfolio, Pricing, FAQ/Contact) with native compositor gesture handling and 100svh viewport locking.
11. **Floating WhatsApp CTA**: Persistent WhatsApp floating button on public pages.
12. **Responsive Layout Shell**: Conditional header/footer rendering (excluded on auth, profile, admin, queue, and access pages).

## Coding Conventions

- Use Functional Components with TypeScript.
- Follow App Router paradigms (Server Components by default, `'use client'` for interactive pages).
- Utility-first CSS with Tailwind 4.
- Modular component design (`sections/`, `layout/`, `features/`, `ui/`, `admin/`).
- Framer Motion for all animations (entry animations, tab transitions, accordion, modals).
- `cn()` helper for conditional class merging.
- Indonesian language for user-facing UI strings (mixed with English for professional sections).
- **Data fetching pattern**: Server Components fetch data via `serverSupabase.ts` functions → pass as `initialData` / `initialXxx` props to `'use client'` section components. Never fetch Supabase data in client-side `useEffect` for public pages.
- **Cache invalidation**: After admin edits, call `revalidateSiteContent()` server action to purge ISR cache.

## Scalability Architecture

- **ISR (Incremental Static Regeneration)**: Homepage, About, and Partnership pages revalidate every 60 seconds. All visitors within that window get instant cached HTML from Vercel Edge.
- **Server-side parallel fetching**: `page.tsx` uses `Promise.all()` to fetch `site_content`, `instagram_posts`, `news`, `gallery`, and `section_visibility` in parallel from Supabase.
- **Server-Side Session Fetching & Claiming**: `/access/[id]` fetches session data server-side via `fetchSessionById()`. The claim mechanism is entirely handled via **Next.js Server Actions (`claimSession`)** using `@supabase/ssr` cookies. This ensures nuclear resilience by inherently batching Supabase API requests via Vercel pool, completely avoiding the 50 concurrent connection leak in Supabase Free Tier. This also features an atomic DB claim update to prevent race conditions.
- **Lazy Authentication Fetch**: Context providers (`AdminEditProvider.tsx`) and components on public pages use `supabase.auth.getSession()` (synchronous zero-latency local check) instead of `supabase.auth.getUser()` (heavy API network check). Unauthenticated visitors trigger 0 API calls for auth.
- **Section visibility**: Resolved server-side — hidden sections are not sent to the client at all, saving bandwidth and render time.
- **Middleware optimization**: Auth check (`supabase.auth.getUser()`) only runs on protected routes (`/profile`, `/admin`). Public routes skip middleware entirely for zero-latency routing.
- **Cache headers**: Static assets (fonts, images, JS/CSS bundles) served with `Cache-Control: public, max-age=31536000, immutable`.
- **Image optimization**: `next.config.ts` whitelists Supabase Storage and Google Cloud Storage domains for `next/image`. Gallery and News sections use `next/image` with automatic WebP/AVIF conversion.
- **Security headers**: `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-DNS-Prefetch-Control: on`, `Permissions-Policy` locked down.
- **SEO metadata**: All public pages export `Metadata` or `generateMetadata()` with title, description, and Open Graph tags.

## Changelog & Continuous Updates (AI Roadmap)

- **April 2026 (Phase 1.5 - Desktop Mesin Kolong Integration)** ✅: The physical Photobooth application now acts as a headless "Mesin Kolong". Desktop Kiosk QR Codes redirect visitors directly to `/access/[id]` for the Photo Claim Mechanism.
- **April 2026 (Phase 2A - Scalability)** ✅: Implemented ISR with 60s revalidation on homepage. Refactored Gallery, InstagramFeed, and News from client-side fetching to server-side props. Added `next.config.ts` with image optimization (Supabase + GCS domains), cache headers for static assets, and security headers. Expanded `revalidateSiteContent()` to cover `/`, `/about`, `/partnership`. Result: ~99% reduction in Supabase API calls (from ~1,200 per 300 visitors to ~5).
- **April 2026 (Phase 2A+ - Scalability Continuation)** ✅: Converted `/about` and `/partnership` from `"use client"` to ISR Server Components with CMS integration (`about_page` / `partnership_page` sections in `site_content`). Converted `/access/[id]` to Server Component with server-side session fetch + `generateMetadata()` for dynamic SEO. Optimized middleware to skip `getUser()` on public routes. Migrated Gallery and News sections from `<img>` to `next/image` for automatic WebP/AVIF. Added SEO metadata exports to About, Partnership, and Access pages.
- **April 2026 (Phase 2A++ - Enterprise Load Balancing Fix)** ✅: Refactored Session Claim function into a Next.js Server Action with an atomic SQL condition (`eq('is_claimed', false)`) to eliminate double-claim race conditions and bypass standard Supabase Free Tier limitations during a 400+ user traffic spike via pooling. Changed unused API-heavy `getUser()` checks in client contexts (like `AdminEditProvider` and `AccessSessionClient`) to `getSession()` to eliminate thousands of 4xx network calls.
- **April 2026 (Phase 2B - Inline Visual Editor)** ✅: Implemented Wix-like live Inline Visual Editor. Super Admins click textual/image elements directly on the live site via `EditableText` / `EditableImage` overlays. Changes save immediately to Supabase `site_content` table. Includes `LayoutEditorModal` (split-panel editor with iframe preview, section visibility, gallery management, zoom 10-300%), `IframeEditBridge` (click-to-edit in iframe via postMessage), and `TextEditModal`.
- **April 2026 (Phase 2C - Mobile UX Perfection)** ✅: Perfecting UI/UX flows specifically for handheld devices. Patched: Header menu lock-body overlay, Hero component viewport height responsiveness (`svh`), Profile strip canvas lazy-rendering (to preserve CPU/Ram on low-end androids), and strictly hiding the Split UI Layout Editor module for small screens.
- **April 2026 (Phase 2D - Gallery Enhancement, News Detail & Section Images)** ✅: Added gallery metadata editor (editable event names, categories, video upload support). Created `/news/[id]` detail page with ISR + dynamic SEO. News cards now link to detail pages with "Baca Selengkapnya" CTA. News image upload with "Sync to Gallery" checkbox auto-adds news images to gallery. Replaced placeholder images in About, Product, Location sections with `EditableImage` components. Added `fetchNewsById()` server fetcher. Updated `fetchGalleryImages()` with video file type detection (`mediaType` field).
- **April 2026 (Phase 2E - Image Crop Modal & Full Inline Editing)** ✅: Added `ImageCropModal` canvas-based image cropper (drag-to-pan, zoom, aspect ratio) that appears before upload in `EditableImage`. Created `EditableArrayItemText` component for inline editing of JSON array items. Made ALL text on the website inline-editable in admin mode: Product items (name, tagline, description), Pricing features & packages (prices, durations), Testimonials (quotes, authors, roles), FAQ (questions, answers), Footer CTA, Header CTA.
- **June 2026 (Phase 3A - Queue Management System)** ✅: Built full digital queue system for photobooth visitors. Key features: (1) `/queue/[eventId]` — consumer takes a queue number via web without requiring login (optional WA number for notifications); (2) `/queue/[eventId]/ticket/[ticketId]` — live ticket display with SVG countdown ring, position-from-front, SSE real-time updates with auto-reconnect + 10s polling fallback, Web Audio API notification sound when called; (3) `/queue/[eventId]/display` — fullscreen TV display mode for venue monitors; (4) Webhook API `/api/queue/webhook` for desktop photobooth app integration (POST `session_started`/`session_completed` events); (5) Operator Dashboard tab in `/admin` — create events, call next, skip, complete, reset, QR code generator, TV display link; (6) SSE broadcast system via `/api/queue/stream/[eventId]`; (7) WhatsApp notifications via Fonnte API (configurable `FONNTE_API_KEY`) sent at position threshold (default: 2 from front); (8) Rolling average session duration calculation (updated on each `session_completed` webhook); (9) Ticket auto-link to `sessions` table via `queue_ticket_id` foreign key. New DB tables: `queue_events`, `queue_tickets`. New env vars: `QUEUE_WEBHOOK_SECRET`, `FONNTE_API_KEY`, `NEXT_PUBLIC_SITE_URL`.
- **June 2026 (Phase 3B - Auth-Gated Queue)** ✅: Queue system now requires login/registration. Anonymous queue tickets are no longer supported. Key changes: (1) Register page enhanced with `full_name` + `phone_number` fields stored in Supabase `user_metadata`; (2) Login & Register pages support `?redirect=` query param for queue flow; (3) `/queue/[eventId]` now checks auth server-side — unauthenticated users see `QueueAuthGate` component with login/register choice; (4) `QueueJoinForm` auto-fills user data from account (no manual input); (5) Join API validates auth and extracts user data from metadata; (6) WhatsApp/Fonnte notification system completely removed from SSE stream; (7) `joinQueue()` now requires `userId` as mandatory parameter.
- **June 2026 (Phase 3C - Web Push + 4-Tier Proximity + Audio Cues)** ✅: Replaced Fonnte WA notifications with Web Push Notifications + visual/audio feedback. Key features: (1) Service Worker (`public/service-worker.js`) for push notification display + click-to-open; (2) Push subscription flow via `pushSubscription.ts` client utilities + `/api/queue/push/subscribe` endpoint; (3) Server-side push sender (`pushSender.ts`) using `web-push` library with VAPID keys; (4) 4-tier proximity system (`QueueProximityTier`): `waiting` 🟢 (5+), `approaching` 🟡 (3-4), `preparing` 🟠 (1-2), `your_turn` 🔴 (called); (5) Redesigned `QueueStatusBadge` with tier-specific colors, pulse animations, shimmer effects; (6) Enhanced `QueueEstimateTimer` with tier-colored countdown ring + detailed estimate breakdown (who's shooting, per-session calculation); (7) Dynamic background gradients on ticket page per tier; (8) Distinct Web Audio API chimes per tier transition: single chime (approaching), double urgent (preparing), triple ascending (your_turn); (9) Webhook sends push notifications on proximity changes + "Foto sudah siap!" on completion; (10) Push toggle button on ticket page. New DB table: `push_subscriptions`. New env vars: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`. New columns on `queue_tickets`: `push_preparing_sent`, `push_approaching_sent`.
- **June 2026 (Phase 3D - QR Scan Session Linking + Auto-Claim)** ✅: Users scan QR code from photobooth screen to link session to their account. Key features: (1) `QRScannerModal` component with camera access via `getUserMedia`, native `BarcodeDetector` API with `jsQR` fallback, viewfinder overlay with animated scan line, torch toggle, success animation; (2) `/api/queue/generate-session-token` — photobooth app generates time-limited QR token (base64url, 10min expiry); (3) `/api/queue/link-session` — validates token, finds user's active ticket, links session to user account, broadcasts SSE update; (4) "SCAN QR UNTUK MULAI" button appears on ticket page when status is `called`; (5) Webhook `session_completed` auto-claims photos to user via `user_id` on ticket → `sessions.user_id` update; (6) Push notification "Foto Kamu Sudah Siap!" sent on auto-claim completion. New column on `queue_tickets`: `session_id`.
- **June 2026 (Phase 3E - Profile Queue Integration)** ✅: Active queue tickets displayed on user's profile page. Key features: (1) `ActiveQueueCard` component shows ticket number, event name, booth, status badge, and links to ticket detail; (2) Profile page fetches active tickets on init and renders them in a dark gradient card above photo gallery; (3) `fetchUserActiveTickets.ts` server-side fetcher for user-specific tickets.
- **June 2026 (Phase 3F - Auto-Advance Queue + Push Notification Fix)** ✅: Queue now fully automated — no manual operator "Next" click needed. Key changes: (1) Webhook `session_completed` now **auto-calls next waiting ticket** (sets status to `called`, sets `called_at` + 5min `expires_at`); (2) Auto-called user receives urgent push notification "GILIRAN KAMU! 🔴" with `requireInteraction: true` and vibrate pattern; (3) **VAPID keys generated and configured** in `.env.local` — previously missing, which caused all push notifications to silently fail; (4) Fixed VAPID subject format from `https://` URL to `mailto:` (required by web-push spec); (5) `QueueTicketDisplay` now **auto-prompts for notification permission** on mount instead of only subscribing when already granted; (6) Operator manual `call_next` action also sends push notification + proximity pushes; (7) Refactored `sendProximityPushNotifications` to use `createServiceClient()` instead of redundant dynamic imports; (8) Fixed `cookieStore.set()` try-catch in operator action route; (9) Fixed `Permissions-Policy` header to allow `camera=(self)` for QR scanner. New env vars configured: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `NEXT_PUBLIC_SITE_URL`, `QUEUE_WEBHOOK_SECRET`. Webhook response now includes `autoCalledNext: boolean` field.
- **June 2026 (Phase 3G - Profile UI Redesign & Brutalism Integration)** ✅: Completely redesigned the `My Photos` (`/profile`) page to adopt the main website's Graphic Brutalism Design System (`bg-white`, `text-primary`, `hard-shadow-black`, strict square borders, `marker-font`). Removed the "Edit Page" (`EditModeToggle`) floating button from the profile page to keep the UI clean for admins and users. Updated `ActiveQueueCard` to match the brutalist aesthetic (squared corners, dark borders, bold solid text) for seamless integration into the redesigned profile layout.
- **June 2026 (Phase 3H - Profile Feed + Gallery Redesign)** ✅: Complete UX overhaul of `/profile` from single-session tab viewer to Instagram-inspired dual-mode. Key changes: (1) **Feed View** — all sessions displayed as vertical-scroll cards (`SessionFeedCard`) with strip hero image, media type indicators (GIF/Live/Photos count), event name, date, and "See More" CTA linking to detail page; (2) **Gallery View** — 2-column grid of all photo strips (`GalleryGrid`) with event overlay and tap-to-navigate; (3) **View toggle** via grid/list icon in top nav bar; (4) **Blue nav bar** (`bg-primary`) with white icons — mobile shows icon-only (Home, Grid toggle, Logout), desktop adds text labels; (5) Removed greeting ("Halo, username"), session dropdown, and bottom tab bar from main profile page; (6) **New `/profile/[sessionId]` page** preserves original tab-based view (Strip/GIF/Live/Photos) scoped to one session with back navigation; (7) **Golden ratio typography** scale applied across all profile pages; (8) `EditModeToggle` updated with `startsWith("/profile")` to hide on all profile sub-routes. New components: `SessionFeedCard.tsx`, `GalleryGrid.tsx`.
- **June 2026 (Phase 3I - Safari Download Proxy & Queue Brutalism)** ✅: Fixed a critical iOS Safari bug where downloading media silently failed due to async Blob/CORS restrictions by building a custom API proxy (`/api/download/route.ts`) that streams the file with a `Content-Disposition: attachment` header, forcing the native iOS download prompt. Completely redesigned the Queue module (`QueueAuthGate`, `QueueJoinForm`, `QueueTicketDisplay`, `QueueStatusBadge`, `QueueEstimateTimer`) to follow the Graphic Brutalism design system (`bg-white`, `border-4 border-black`, `hard-shadow-black`, strict block colors, marker fonts), replacing the old sleek dark glassmorphism theme. Refined Queue UI to remove excessive borders on headers/info and removed emojis for a cleaner look.
- **June 2026 (Phase 3J - Super Admin Gallery Authority)** ✅: Super Admin users can now view ALL photo sessions from ALL users in their `/profile` gallery page. Key changes: (1) Profile page detects super admin status using the same dual-validation pattern (env `NEXT_PUBLIC_ADMIN_EMAILS` + `admins` table `is_super` flag); (2) New scope toggle button (User/Users icon) in nav bar lets super admins switch between "My Photos" (own sessions only) and "All Sessions" (every session in the database); (3) Yellow info banner shows total session count when in "All Sessions" mode; (4) `SessionFeedCard` gains `showOwner` prop — displays truncated `user_id` on each card in admin mode; (5) Title dynamically changes between "MY PHOTOS" and "ALL SESSIONS" with Super Admin badge; (6) Queue tickets only shown in "mine" scope to avoid clutter; (7) Empty state message adapts to current scope.
- **June 2026 (Phase 3K - Profile Performance Optimization)** ✅: Major performance overhaul for the `/profile` gallery to run smoothly on low-end mobile devices. Key changes: (1) **Pagination with infinite scroll** — sessions load 10 at a time via `range()` query, with `IntersectionObserver` sentinel element triggering next page fetch 400px before scroll end; (2) **next/image migration** — replaced all raw `<img>` tags in `SessionFeedCard`, `GalleryGrid`, and `/profile/[sessionId]` with `next/image` (`NextImage`) for automatic WebP/AVIF conversion, responsive `sizes` hints, and quality optimization (60-80); (3) **Feed card eager/lazy split** — first 2 cards load eagerly, rest are lazy; gallery thumbnails: first 4 eager, rest lazy; (4) **Animation delay cap** — `Math.min(index * 0.08, 0.4)` prevents animation queue buildup with many items; (5) **Super admin session detail access** — super admins can view any user's session detail page (removed `user_id` filter for admins); (6) **Canvas strip data URL passthrough** — client-generated strip images (data: URLs) bypass `next/image` to avoid unnecessary processing; (7) **GIF kept as raw img** — animated GIFs bypass `next/image` which would convert them to static frames; (8) **"Semua sesi telah dimuat" end indicator** — shows when pagination is exhausted.
- **June 2026 (Phase 3L - Client Memory & Resource Optimization)** ✅: Implemented deep optimizations for mid-tier mobile devices: (1) Migrated all client canvas strip renders in `/profile/[sessionId]` and `FrameEditorModal` from data URLs to lightweight blob object URLs with proper cleanup hook `URL.revokeObjectURL` to eliminate RAM leaks; (2) Fixed client canvas downloads for local data/blob URLs by bypassing `/api/download` server proxy; (3) Optimized QR Scanner (`QRScannerModal`) scan loop by pre-loading and caching `jsQR` module rather than repeating dynamic import promise resolution on every 250ms frame; (4) Added `GalleryVideoItem` component with `IntersectionObserver` to auto play/pause visual archive videos only when in-view and set `preload="none"`; (5) Viewport-lazy loaded Instagram embed scripts and iframes in `InstagramFeed` to only trigger when scrolling close (400px margin); (6) Replaced the heavy, CPU-intensive fluid animated background gradient (`bg-fluid-gradient`) with a static brutalist gradient (`linear-gradient(135deg, #F9F9F9 0%, #EAEAEA 100%)`) to completely eliminate GPU repaint lag and stutters; (7) Disabled the heavy SVG turbulence filter noise (`paper-texture`) on mobile screens (< 768px) to reduce mobile browser fill-rate overhead; (8) Simplified Framer Motion transitions between feed/gallery view modes on the profile page to pure opacity fades to prevent layout stutters during scrolling; (9) Optimized FAQ accordion card hover styling (`FAQ.tsx`) by introducing dynamic card lifting transformations (`md:hover:-translate-y-1 md:hover:-translate-x-1`) and hover shadows matching the brutalist theme cycle (`hoverShadows`); (10) Refactored the FAQ toggle icons to render a single, smoothly-rotating Plus icon (`rotate-45`) on expansion instead of instant flashes between Plus and Minus icons; (11) Redesigned the homepage gallery (`Gallery.tsx`) from a staggered masonry columns layout to a strict, row-aligned CSS Grid (3 columns on desktop/laptop) with uniform portrait aspect ratios (`aspect-[9/16]`) matching YouTube Shorts desktop view; (12) Rendered translucent target media type badge overlays (Play icon for videos, Camera icon for photos) in the top-right corner of each gallery card; (13) Added dynamic card lifts, zoom animations on hover (`group-hover:scale-105`), and smooth overlay text slide-ups.
- **June 2026 (Phase 3M - Dynamic Orientation & Mobile Rotation Fix)** ✅: Fixed a layout bug on mobile devices where the layout defaulted to horizontal (landscape) and locked due to forced body rotation. Added mobile user-agent checking in `OrientationProvider` to disable forced rotation on mobile screens, and wrapped the `data-orientation="portrait"` CSS style in `globals.css` with a `@media (min-width: 1024px)` query to allow natural, dynamic screen rotation on handheld devices.
- **June 2026 (Phase 3N - Claim Page Graphic Brutalism Redesign)** ✅: Redesigned the QR access point claim page component `AccessSessionClient.tsx` to fully adopt the website's Graphic Brutalism design system. Replaced standard gray-50 background with brutalist static gradient (`bg-fluid-gradient paper-texture`), redesigned preview items with thick black borders, dynamic hover transformations, and hard shadows (`hard-shadow-black`), and overhauled the CTA block with strong rectangular borders, high-contrast typography, and tactile button offsets.
- **June 2026 (Phase 3O - Context Verification)** ✅: Read and verified the `agents.md` file to align on the current project status and architecture rules.
- **June 2026 (Phase 3P - Hover Performance Optimization & SVG-to-PNG Migration)** ✅: Resolved severe hover animation lag and FPS drops across all sections by replacing the highly CPU-intensive dynamic SVG `<feTurbulence>` fractal noise overlay (`.paper-texture`) with a lightweight repeating static PNG noise texture (`/images/paper-noise.png`). Migrated accordion collapse animations in [FAQ.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FAQ.tsx) and [Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx) from unoptimized CSS Grid row transitions to hardware-accelerated Framer Motion (`AnimatePresence` + `motion.div` height/opacity animations) and replaced their parent container `transition-all` declarations with targeted `transition-[transform,box-shadow]` styles. Further optimized rendering by migrating from 10+ local section-level absolute texture overlays to a single global fixed overlay (`body::after` in [globals.css](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/globals.css)), completely eliminating layout repaint overhead when elements resize. Centered the entire FAQ section layout and text (heading, accordion container, questions, and answers content). Redesigned the site header in [Header.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/layout/Header.tsx) into a floating, dynamic glassmorphism rounded rectangle nav bar with active page/hash tracking that highlights links in solid orange when clicked.
- **June 2026 (Phase 3Q - Floating Header Glassmorphism Transparency & Shimmer Animation)** ✅: Increased transparency of the floating header by reducing background opacity (to `bg-white/25` when scrolled and `bg-white/12` when not scrolled) and increasing backdrop blur (`backdrop-blur-lg` / `backdrop-blur-md`). Added a hardware-accelerated CSS keyframe glass shine animation (`@keyframes glass-shimmer` inside `globals.css`) that moves a translucent white gradient skew overlay from left to right across the navigation bar every 7 seconds. Implemented these elements inside separate stacking-context layers (`z-[-2]` for glass background, `z-[-1]` for shine animation overlay, and `relative z-10` for navigation links and logo) to ensure text readability and link interactability.
- **June 2026 (Phase 3R - Textured Global Base Background)** ✅: Migrated the entire website's background base from solid white to a full-screen covered background using `main-background.png` (originally `main background.png` in the project root, copied to `public/images/main-background.png`). Updated `globals.css` to define the body background image (`background-repeat: no-repeat`, `background-size: cover`, `background-attachment: local`, `background-position: top center`) and set outer container backgrounds to `bg-transparent` in all key section files (`About.tsx`, `Product.tsx`, `Pricing.tsx`, `Testimonials.tsx`, `Gallery.tsx`, `InstagramFeed.tsx`, `FAQ.tsx`, `News.tsx`) and page-level layouts (`AboutContent.tsx`, `PartnershipContent.tsx`), while keeping internal modules, cards, and text containers solid white or gray to ensure high readability and design contrast.
- **June 2026 (Phase 3S - Featured Frames Showcase & Detail Page Expansion)** ✅: Added a "Featured Frames" section to the homepage and built a subpage (`/frames`) displaying photobooth frame templates in a centered 3-column grid. Key features: (1) Homepage auto-sliding deck carousel showing featured frame designs with navigation controls; (2) Dedicated `/frames` subpage with category filters, a fuzzy search bar, and an interactive zoom/lightbox with direct download options; (3) Premium modern glassmorphic visual theme (`bg-white/12 backdrop-blur-lg border border-white/20 hover:bg-white/20 rounded-3xl shadow-sm`) for frame card elements; (4) Full CMS Admin Tab under `/admin` to list, delete, and upload frame templates dynamically via Supabase storage and `site_content` JSON persistence; (5) Cache revalidation integration within `revalidateSiteContent()` server action to sync updates to static pages instantly.
- **June 2026 (Phase 3T - Featured Frames Compact Marquee & Dynamic Aspect Ratio)** ✅: Completely rebuilt the Featured Frames homepage section from a step-by-step carousel into a compact infinite-loop marquee slider. Cards are now significantly smaller (~140px mobile / ~170px desktop), the frame track duplicates all items and uses a seamless CSS `marquee-scroll` keyframe animation that continuously scrolls left and loops infinitely. Animation pauses on hover. Replaced fixed `aspect-[3/4]` containers with dynamic aspect ratios that adapt to each uploaded image's natural dimensions via `onLoad` → `naturalWidth/naturalHeight`. Removed manual prev/next nav buttons and progress dots in favor of the auto-scrolling design. Added fade-edge gradients on both sides of the track for a polished look. Applied consistently to `FeaturedFrames.tsx` (homepage) and `FramesGalleryClient.tsx` (gallery grid + lightbox).
- **June 2026 (Phase 3U - Super Admin Gallery Removal & Featured Frames UX)** ✅: Removed the Super Admin "view all sessions" feature from the profile system. Key changes: (1) Removed `isSuperAdmin` state, `sessionScope` state, scope toggle button (User/Users icon), yellow info banner, and `ShieldCheck` badge from `/profile` page; (2) Profile page now always queries `sessions` filtered by `user_id` — no admin bypass; (3) Session detail page (`/profile/[sessionId]`) removed super admin check and always filters by `user_id`; (4) Removed `showOwner` prop usage from `SessionFeedCard` in profile; (5) Simplified imports by removing `ShieldCheck`, `Users`, `User` from lucide-react. Also improved Featured Frames marquee: (a) Fixed infinite loop by repeating frames 8× minimum per half to guarantee viewport overflow even with 1-2 items; (b) Marquee cards now show image only — removed title/category text footer; (c) Clicking a card opens a brutalist detail popup modal with frame image, title, category badge, and "Lihat Detail" CTA; (d) Marquee auto-pauses when popup is open.
- **June 2026 (Phase 3V - Vercel Image Optimization Cost Control & Lightbox Fix)** ✅: Configured `minimumCacheTTL` to 1 year (`31536000` seconds) and added format optimization (`image/avif`, `image/webp`) in `next.config.ts`. This ensures Vercel caches optimized assets indefinitely instead of re-optimizing on subsequent requests, preventing excess optimization charges for photobooth pictures and static elements. Also resolved a build error in `FramesGalleryClient.tsx` where a `span` tag was incorrectly closed with a `h2` tag. Fixed the homepage "Featured Frames" marquee loop animation by migrating style definitions from scoped `<style jsx>` tags to the global stylesheet (`globals.css`) for Next.js 16 / Turbopack compatibility, refactoring it to use a `.animate-marquee` class combined with custom CSS properties for dynamic speed parameters. Refactored the marquee track from `gap-4` spacing to individual item `mr-4` margins to eliminate a microscopic 8px layout shift when the animation translates by `-50%`, achieving a pixel-perfect, completely seamless infinite sliding loop. Increased the card widths on the featured frames marquee from `w-[140px]/w-[170px]` to `w-[180px]/w-[250px]` (margin increased to `mr-6`) to make the frame templates look larger, clearer, and more visually dominant on the homepage. Wrapped the `<FramesGalleryClient>` element inside a `<Suspense>` boundary in `src/app/frames/page.tsx` to prevent Next.js static prerendering bailout when compiling pages using `useSearchParams()`.
- **July 2026 (Phase 3W - About Us / Origin Story Removal & Nav Cleanup)** ✅: Removed the About Us section and Origin Story from the website. Key changes: (1) Removed `<About>` component rendering and import from homepage `page.tsx`; (2) Removed "ABOUT" nav item (`/about` link) from `Header.tsx` navigation — nav now shows: PRODUCT, PRICING, GALLERY, PARTNERSHIP, MY PHOTOS, BOOK NOW; (3) Removed `/about` revalidation from `revalidateSiteContent()` server action in `actions.ts`. Note: The `/about` route files (`about/page.tsx`, `AboutContent.tsx`) and `About.tsx` component are kept on disk but no longer referenced or rendered anywhere.
- **July 2026 (Phase 3X - Hero Text Customization)** ✅: Updated the hero title to "Seboothkan Momenmu Bersama Sebooth" and the CTA button text to "Sebooth-in Moemenmu Sekarang!" in `Hero.tsx` to align with the client's request.
- **July 2026 (Phase 3Y - Typography Migration to Poppins)** ✅: Migrated all fonts (body, headlines, marker/handwritten elements, and custom brand typography) to Poppins globally by mapping `--font-headline`, `--font-marker`, and `--font-sebooth` to `var(--font-poppins)` in `globals.css` and the Tailwind theme config.
- **July 2026 (Phase 3Z - Our Services Glassmorphism Redesign)** ✅: Redesigned the "Our Services" (Product) section. Updated the title to "Sebutin Apa Yang Loe Mau!" and replaced the product cards with three rounded glassmorphic cards (bypassing the brutalist zero-border-radius constraint in `globals.css`) containing placeholder icons (`Camera`, `Sparkles`, `Handshake` from `lucide-react`) and updated copy. Added vertical flashcard dimensions (min-height, centered alignments, restricted text widths, and enlarged icon placeholders) for an ideal physical-card aesthetic.
- **July 2026 (Phase 4A - Context Read & Synchronization)** ✅: Read `AGENTS.md` file to synchronize AI context and confirm project guidelines and current roadmap status.
- **July 2026 (Phase 4J - Product Section dontboardme OUR SERVICES Auto-Rotating Carousel)** ✅: Transformed "SEBUTIN APA YANG LOE MAU!" section (`Product.tsx`) to replicate `dontboardme.com`'s OUR SERVICES layout with automatic slide rotation. Key changes: (1) Added 4.5s automatic slide timer (`setInterval`) with hover-pause functionality to rotate through `01. BATCH BOOKING PACKAGE`, `02. ALL YOU CAN PHOTOS`, and `03. JADIIN SEBOOTH PARTNER LOE`; (2) Rendered giant step numbers (`01`, `02`, `03`) on the left side of the display card in `Bayon` font; (3) Added floating accent ball on the section title; (4) Built concentric background circle layout with Sebooth Deep Blue (`#002366`) and Orange (`#ff4500`).
- **July 2026 (Phase 4K - AI Context Synchronization)** ✅: Read and verified `agents.md` file upon user request to align AI context with current project architecture, stack, design system, and execution history.
- **July 2026 (Phase 4L - AI Context Verification & Sync)** ✅: Read and verified `agents.md` to synchronize AI context with project guidelines, architecture, and execution history.
- **July 2026 (Phase 4M - Session Detail Photo Strip Load & Fail-safe Fix)** ✅: Resolved broken photo strip image issue in `/profile/[sessionId]` by updating media type helpers (matching all strip/gif/live formats), adding fallback logic to load raw images when Next.js `NextImage` optimization fails, and refining multi-tier image loader in canvas template generator.
- **August 2026 (Phase 4N - AI Context Read & Sync)** ✅: Read and verified `AGENTS.md` file upon user request to align AI context with current project architecture, guidelines, and execution history.
- **August 2026 (Phase 4O - Hero & Product Badge Removal)** ✅: Removed `⚡ ZERO-LAG PHYSICAL KIOSK SYSTEM` badge from [Hero.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Hero.tsx) and removed `AUTO-ROTATING SERVICE` badge & auto-switch timer from [Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx) per user request.
- **August 2026 (Phase 4P - Section Black Border & Background Seamless Integration)** ✅: Removed all thick black top/bottom section borders (`border-t-8 border-black`) across Testimonials, News, InstagramFeed, FeaturedFrames, FAQ, and About sections. Converted solid section background colors (`bg-[#f8f9fa]`) in Product, ProcessPinningSection, Pricing, and Gallery to `bg-transparent` for a continuous visual flow with the global textured background (`main-background.png`).
- **August 2026 (Phase 4Q - Background Circle Softening & Edge Removal)** ✅: Replaced giant solid background accent circles in `Product.tsx`, `ProcessPinningSection.tsx`, and `Pricing.tsx` with soft, blurred ambient glows (`blur-[120px]`/`blur-[140px]`) to eliminate sharp visual edge cutoffs between "SEBUTIN APA YANG LOE MAU!" and "TRUSTED BY MANY". Removed remaining `border-y-8 border-black` in `AboutContent.tsx`.
- **August 2026 (Phase 4R - AI Context Read & Sync)** ✅: Read and verified `AGENTS.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 4S - GitHub Repository Synchronization)** ✅: Staged, committed, and pushed all recent section styling updates, background glow refinements, image assets, and documentation changes to GitHub repository.
- **August 2026 (Phase 4T - Live Photo Classification & Frame Template Ordering Fix)** ✅: Fixed live photo misclassification bug where pose photo files containing `live` in their URL/filename (e.g. `live_photo_1.jpg`) were incorrectly excluded from `photos` and mistaken for video items, causing frame template rendering to fail or fall back to the first photo. Added natural numerical sorting to session media items (`photo_1`, `photo_2`, `photo_3`) and implemented multi-live photo video carousel navigation (`getLives()`) on the Live tab.
- **August 2026 (Phase 4U - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 4V - Full-Page Dynamic Responsive Image Slide Deck Landing Page)** ✅: Rebuilt the landing page (`/`) into an ultra-lightweight, high-performance vertical full-page slide deck (`SlideDeckLanding.tsx`, `SlideItem.tsx`, `SlideNavigationDots.tsx`, `landingSlides.ts`). Key features: (1) Hardware-accelerated CSS vertical scroll snapping (`scroll-snap-type: y mandatory`) with keyboard arrows, PageUp/Down, and touch swipe gestures; (2) Responsive desktop (`16:9` 1920×1080) and mobile (`9:16` 1080×1920) image loader with auto-fallback to high-contrast SVG placeholders when PNGs are pending; (3) Right-side floating dot navigation with hover slide tooltips and active slide indicator; (4) Interactive overlay CTAs for booking, WhatsApp consultation, and frame catalog; (5) Automatic lazy-loading optimization for minimal bandwidth and 60fps performance.
- **August 2026 (Phase 4W - Turbopack CSS Syntax Fix & Dev Server Verification)** ✅: Fixed invalid CSS custom property syntax in `globals.css` (`--c-[#002366]` / `--c-[#ff4500]`) which caused Turbopack parser to throw `Unexpected token SquareBracketBlock`. Synchronized SVG fallback placeholder assets to prevent Next.js image loader errors when PNGs are pending. Verified `npm run dev` and `npm run build` running with HTTP 200 OK status.
- **August 2026 (Phase 4X - Direct Full-Slide Deck Transition on Scroll/Swipe)** ✅: Upgraded landing page slide system from passive scroll-snap to active discrete slide deck transitions. Key changes: (1) Disabled Lenis smooth-scroll on homepage route (`/`) in `SmoothScrollProvider` and removed footer in `LayoutShell` on `/` to lock container to exact 100svh; (2) Implemented `translate3d(0, -${activeIndex * 100}%, 0)` slide track with cubic-bezier easing (`cubic-bezier(0.65, 0, 0.35, 1)`); (3) Added debounced event listeners for mouse wheel (`wheel`), touch gestures (`touchstart`/`touchend` with 35px threshold), and keyboard arrows/page controls so every scroll gesture immediately advances exactly one slide.
- **August 2026 (Phase 4Y - SSR Hydration Sync & Force-Dynamic Mode)** ✅: Eliminated Next.js hydration mismatch on homepage by synchronizing initial SSR markup with client hydration (`mounted` state cycle in `SlideDeckLanding.tsx`), removed redundant outer wrapper div, and set `export const dynamic = 'force-dynamic'` in `page.tsx` to ensure real-time client-server DOM equivalence in dev mode. Verified `npm run build` compiled 100% cleanly.
- **August 2026 (Phase 4Z - Animated Panning Hero Slide & Centered Overlay)** ✅: Implemented dynamic Hero on Slide 1 using source assets from `picture_stocks/`: (1) Infinite seamless horizontal background panning animation (`bg_slide_1.png` rendered via `.animate-pan-left` at 40s linear loop from right to left); (2) High-contrast darkened vignette and backdrop gradients for visual depth; (3) Perfectly calibrated centered foreground overlay (`overlay_slide_1.png`) with refined responsive scaling (`scale-100` up to `scale-120`, max-height `76vh`) and deep drop shadow (`drop-shadow-[0_28px_65px_rgba(0,0,0,0.92)]`); (4) Preserved floating WhatsApp reservation CTA button below the hero graphic.
- **August 2026 (Phase 5A - Header Glassmorphism Capsule Redesign & Slide Badge Removal)** ✅: Enhanced top navigation and slide UI: (1) Removed the `Slide 01 / 6` top-right badge from `SlideDeckLanding.tsx`; (2) Redesigned `Header.tsx` into a modern floating capsule shape (`rounded-full`, max-w-5xl) with ultra-high frosted glass blur (`backdrop-blur-2xl bg-black/50 border border-white/20`); (3) Replaced dull navy text with high-contrast bright white typography, golden accents for MY PHOTOS, and luminous orange active indicators (`#FF4500` glow); (4) Styled mobile dropdown with `rounded-3xl` and matching high-blur glassmorphism.
- **August 2026 (Phase 5B - Hero CTA Position Elevation)** ✅: Elevated the "SEBOOTH-IN SEKARANG" floating CTA button on Slide 1 to `bottom-20 sm:bottom-24 md:bottom-28` to provide clean visual separation from the bottom scroll-down indicator cue and improve thumb reachability on mobile devices. Verified production build passed cleanly.
- **August 2026 (Phase 5C - CTA Position Fine-Tuning)** ✅: Re-positioned the "SEBOOTH-IN SEKARANG" CTA button to ~1/4 slide height from the bottom (`bottom-[14vh] sm:bottom-[16vh] md:bottom-[18vh]`) and adjusted the scroll-cue indicator down to `bottom-3 sm:bottom-4` for optimal ergonomic balance and visual hierarchy.
- **August 2026 (Phase 5D - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 5E - Spatial Vision Pro Product Gallery & Gambar 2 Card Model Redesign)** ✅: Completely rebuilt `Product.tsx` into a 3D Spatial Coverflow Gallery inspired by Apple Vision Pro UI (Gambar 1) with custom sliding card items (Gambar 2). Key features: (1) Floating top glassmorphic header capsule (`SeboothProductGallery.com` URL pill, view toggle, navigation arrows, share button); (2) Left-side vertical floating glass tool pill (`Palette`, `Heart`, `User` icons); (3) 3D Coverflow Spatial Carousel (`perspective: 1200px`) with 3D Y-axis rotation, depth Z-translation, scale, and opacity fading for non-active cards; (4) Replication of Gambar 2 card model (`rounded-[36px]`, dark frosted gradient overlay, high-res photobooth imagery, title/description typography, top controls with `Expand` modal button); (5) High-contrast **Orange-to-Purple Gradient Pill CTA** (`from-[#FF5E00] via-[#FF3900] to-[#551286]`) with text *"Mau ini dong"* linking directly to WhatsApp consultation; (6) Bottom floating glass control pill with avatar thumbnail of active product, title/subtitle, search detail, and navigation arrows; (7) Interactive Expand Detail Modal with full feature breakdown; (8) Embedded `<Product />` directly into Slide 2 (`services`) in `SlideItem.tsx` and added `images.unsplash.com` to `next.config.ts`.
- **August 2026 (Phase 5F - Progressive Blur & Tall Portrait Card Redesign)** ✅: Redesigned product cards to use tall vertical portrait orientation (`w-[340px-365px] h-[660px-700px]`, `rounded-[40px]/rounded-[48px]`) instead of wide horizontal cards. Implemented Apple-style Multi-Layer Progressive Backdrop Blur system (`backdrop-blur-[3px]`, `backdrop-blur-[10px]`, `backdrop-blur-[20px]`, `backdrop-blur-[36px]` stacked with stepped gradient mask images `linear-gradient`) to create a smooth, seamless transition from the top photo to the bottom card content without harsh border lines.
- **August 2026 (Phase 5G - Card Height Calibration & Compact Ratio)** ✅: Calibrated card height to compact portrait dimensions (`w-[260px-350px] h-[420px-540px]`, `rounded-[32px]/rounded-[36px]`) to reduce excess vertical height while preserving the vertical portrait card shape and progressive blur effect.
- **August 2026 (Phase 5H - Product Showcase Badge Removal)** ✅: Removed the `✦ SEBOOTH SPATIAL PRODUCT SHOWCASE ✦` top badge pill above the section title in `Product.tsx` per user request for a cleaner headline presentation.
- **August 2026 (Phase 5I - White Background & Abstract Blue/Orange Glow Blobs)** ✅: Updated `Product.tsx` background to clean white (`bg-white`) featuring 4 dynamic abstract blurred blobs in Sebooth Deep Blue (`#002366`), Vibrant Orange (`#FF5E00`/`#FF4500`), and Electric Cyan (`#3B82F6`) with high blur radius (`blur-[120px]–[160px]`). Adapted top header, left tool pill, section title (`#002366`), and bottom control pill to match the light theme aesthetic.
- **August 2026 (Phase 5J - Infinite Circular Loop Carousel)** ✅: Upgraded product coverflow carousel math in `Product.tsx` to an infinite circular ring calculation (`normalizedActiveIdx` + shortest path `diff` wrap). The cards now loop continuously in 3D space when clicking next/prev, dot indicators, touch swiping, or clicking side cards without ever hitting a start or end boundary.
- **August 2026 (Phase 5K - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 5L - Slide 2 Product Auto-Play Loop)** ✅: Implemented automatic sliding card loop on Slide 2 (`Product.tsx`). Added 3.5-second `setInterval` auto-rotation combined with hover pause (`isHovered`) and modal pause (`selectedProduct`), enabling continuous hands-free looping across product cards.
- **August 2026 (Phase 5M - Custom Product Card Images & Catalog Update)** ✅: Copied custom product images from `picture_stocks/` (`mini_studio_booth.png`, `vending_machine_booth.png`, `partner_sebooth.png`) into `public/images/products/` and updated `Product.tsx` catalog items to render high-res local brand assets for Mini Studio Photobooth, Vending Machine Photobooth, and Jadi Partner Loe.
- **August 2026 (Phase 5N - Expand Modal Portal Fix & Card Dimension Elevation)** ✅: Fixed expand detail modal position bug by wrapping `<AnimatePresence>` in React `createPortal(..., document.body)` with `z-[9999]`, escaping transformed slide track parents (`translate3d`/`perspective`). Enlarged product cards (`w-[285px-400px] h-[450px-570px]`) and recalibrated 3D X-spread spacing (`translateX = diff * 240`) for a bolder spatial aesthetic.
- **August 2026 (Phase 5O - Bottom Control Pill Removal)** ✅: Removed the bottom floating glass control pill (`< [Avatar Title Subtitle] Search >`) from `Product.tsx` per user request for a cleaner, unobstructed view of the 3D coverflow carousel, keeping clean dot pagination indicators underneath.
- **August 2026 (Phase 5P - Top Navigation Bar Removal)** ✅: Removed the top floating glass capsule navigation bar (`SeboothProductGallery.com`) and left floating tool pill from `Product.tsx` per user request, leaving Slide 2 clean and focused exclusively on the product section title and 3D coverflow card carousel.
- **August 2026 (Phase 5Q - 2-Column Split Layout Redesign)** ✅: Transformed Slide 2 (`Product.tsx`) into a dynamic 2-column split layout (`grid-cols-1 lg:grid-cols-12`). Left column features left-aligned title *"SEBUTIN APA YANG LOE MAU!"*, service badge, tagline text, and navigation arrow/dot controls. Right column showcases the 3D Coverflow spatial card carousel.
- **August 2026 (Phase 5R - Floating Header Clearance Adjustment)** ✅: Added top padding (`pt-24 sm:pt-28 md:pt-32 lg:pt-36`) to `Product.tsx` section container to shift all Slide 2 content down, ensuring zero visual overlap with the site's floating top glassmorphism navigation header.
- **August 2026 (Phase 5S - Our Services Badge Removal)** ✅: Removed the `✦ OUR SERVICES & BOOTHS` top badge pill from the left column in `Product.tsx` per user request for a cleaner headline presentation.
- **August 2026 (Phase 5T - 5s Auto-Play Timer & Card Hover Pause)** ✅: Configured Product 3D card carousel to automatically advance every 5 seconds (`5000ms`). Added card-level `onMouseEnter` / `onMouseLeave` listeners to pause the auto-slide timer immediately when hovering any product card.
- **August 2026 (Phase 5U - Left Column Navigation Controls Removal)** ✅: Removed the left/right arrow buttons and pagination dots from the left column in `Product.tsx` per user request, leaving a ultra-minimalist left column with only the title and subtitle description.
- **August 2026 (Phase 5V - Trusted By Partner Logo Infinite Marquee)** ✅: Added a "TRUSTED BY" section below the title in Slide 2 (`Product.tsx`). Copied all 6 partner logos (`logo_1.png` to `logo_6.png`) from `picture_stocks/logo-partner` into `public/images/partners/` and built a 20-second hardware-accelerated infinite horizontal marquee loop (`.animate-marquee-left`) with hover-pause functionality and edge gradient fades.
- **August 2026 (Phase 5W - Partner Logo Sizing & Tight Spacing Elevation)** ✅: Enlarged partner logo dimensions (`h-11 sm:h-14 md:h-16 w-28 sm:w-36 md:w-40`) and tightened horizontal marquee spacing (`gap-3.5 sm:gap-4.5 md:gap-5`) per user request for prominent, high-impact brand visibility.
- **August 2026 (Phase 5X - Logo 6 Custom Scale Alignment)** ✅: Added custom CSS scale transform (`scale-[1.45] sm:scale-[1.55]`) specifically to `logo_6.png` in `Product.tsx` marquee. Enlarged visual logo size by 50-55% while preserving identical container dimensions & track spacing for 100% uniform layout balance.
- **August 2026 (Phase 5Y - Mobile Viewport & Touch Gesture Optimization)** ✅: Optimized Slide 2 (`Product.tsx`) for mobile screens (< 640px). Calibrated mobile section padding (`pt-16 pb-6 px-3`), headline font scale (`text-3xl`), 3D coverflow X-spread (`translateX = diff * 110`), card dimensions (`w-[245px] h-[390px]`), and modal viewport bounds (`max-h-[88vh]`) for a seamless native mobile experience.
- **August 2026 (Phase 5Z - JSX Map Bracket Fix)** ✅: Fixed missing `})` bracket for `PRODUCTS.map` loop at line 403 in `Product.tsx`, resolving the Next.js Turbopack ECMAScript parse build error.
- **August 2026 (Phase 6A - Slide 3 Vibrant Horizontal Card Carousel & Strict Anti-Download Protection)** ✅: Created [`FramesSlider.tsx`](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FramesSlider.tsx) and mounted it in [`SlideItem.tsx`](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/slides/SlideItem.tsx) for Slide 3 (`id: "frames"`). Features Lay's-style vibrant horizontal sliding cards with auto-rotation (4.5s), category badges, and pop-up frame preview modal. Implemented strict anti-download protection (`onContextMenu={e.preventDefault()}`, `onDragStart={e.preventDefault()}`, `userSelect: none`, `-webkit-touch-callout: none`, transparent shield overlay `z-20`, and diagonal watermark overlay) preventing image downloading via long-press, right-click, or dragging on mobile/desktop.
- **August 2026 (Phase 6B - 35 Photostrip Stock Catalog Integration & Badge Pill Removal)** ✅: Removed top `EXCLUSIVELY DESIGNED FOR YOUR MOMENTS` badge pill from `FramesSlider.tsx`. Copied all 35 photostrip stock images (`1.1.png` to `14.2.png`) from `picture_stocks/photostrip-stock` into `public/images/frames/` and populated the full 35-item catalog into `FRAME_ITEMS` with unique vibrant card theme gradients, categories, titles, and copy protection.
- **August 2026 (Phase 6C - Continuous Non-Ending Marquee & Simplified Card UI)** ✅: Simplified card layout in `FramesSlider.tsx` by removing heavy text titles, descriptions, and feature lists. Converted horizontal slider into a 50-second continuous non-stopping infinite marquee loop (`.animate-marquee-frames`) with edge gradient masks and hover preview modal with full anti-download copy protection.
- **August 2026 (Phase 6D - Marquee Speed Calibration to 140s)** ✅: Adjusted `.animate-marquee-frames` animation duration from 50s to **140s** in `globals.css` per user request, delivering a smooth, relaxed, and elegant sliding pace across the 35 photostrip cards with hover-pause functionality.
- **August 2026 (Phase 6E - Card Dimensions Expansion & Uncropped Photostrip Fit)** ✅: Widened card dimensions to `w-[260px] sm:w-[320px] md:w-[360px]` and increased image container height to `h-[340px] sm:h-[410px] md:h-[440px]` in `FramesSlider.tsx`. Switched image rendering to `object-contain` to display 100% of the photostrip frame image resolution without cropping.
- **August 2026 (Phase 6F - Letterbox Gap Elimination & Snug Edge-to-Edge Photostrip Fit)** ✅: Re-engineered card container aspect ratios and switched image rendering to `object-cover object-center` in `FramesSlider.tsx`, completely eliminating empty top and bottom background gaps around photostrip images.
- **August 2026 (Phase 6G - Vertical Card Height Reduction & Compact Viewport)** ✅: Compacted vertical height of photostrip image container (`h-[210px] sm:h-[250px] md:h-[280px]`) and card dimensions (`w-[190px] sm:w-[230px] md:h-[260px]`) in `FramesSlider.tsx` per user request, delivering a sleek, compact horizontal strip.
- **August 2026 (Phase 6H - Horizontal Card Width Expansion to Widescreen Format)** ✅: Widened horizontal card width to `w-[280px] sm:w-[360px] md:w-[410px]` and image height `h-[190px] sm:h-[230px] md:h-[260px]` in `FramesSlider.tsx` per user request, delivering a spacious landscape card presentation.
- **August 2026 (Phase 6I - Color-Matched Card Backgrounds & Gradient Harmonization)** ✅: Re-calibrated `cardBgGradient` and `badgeBg` across all 35 items in `FRAME_ITEMS` in `FramesSlider.tsx` to match the exact dominant color palette of each photostrip stock image (e.g., deep sea teal `from-[#0f5257] via-[#0b7a75] to-[#197278]` for `4.1.png` BEM FT ocean waves).
- **August 2026 (Phase 6J - Header Title Centering & Tight Marquee Proximity)** ✅: Changed section container flex layout to `justify-center items-center gap-3 sm:gap-5` and reduced header top padding/margins in `FramesSlider.tsx` per user request, drawing the title and subtitle into the center viewport directly above the sliding cards.
- **August 2026 (Phase 6K - CSS Mask Alpha Gradient for Seamless Edge Dissolve)** ✅: Replaced solid dark gradient overlay boxes with hardware-accelerated CSS `maskImage: linear-gradient(to right, transparent 0%, rgba(0,0,0,1) 10%, rgba(0,0,0,1) 90%, transparent 100%)` on marquee container in `FramesSlider.tsx`, removing harsh cutoffs and allowing sliding cards to smoothly dissolve into transparency at the screen edges.
- **August 2026 (Phase 6L - Modal Lightbox Category Badge Removal)** ✅: Removed the category badge pill (`selectedFrame.category`) from the right-side metadata column of the pop-up modal lightbox in `FramesSlider.tsx` per user request.
- **August 2026 (Phase 6M - JELAJAHI FRAME GALLERY Floating CTA Removal)** ✅: Removed the floating gold CTA button (`JELAJAHI FRAME GALLERY ↗`) from Slide 3 (`frames`) in `landingSlides.ts` and `SlideItem.tsx` per user request for a clean, distraction-free view.
- **August 2026 (Phase 6N - Expanded Uncropped Modal Image Preview Area)** ✅: Widened modal container to `max-w-5xl`, expanded left image preview column to `md:w-[58%] lg:w-[62%]`, and switched image fit to `object-contain p-2 sm:p-3` in `FramesSlider.tsx`, ensuring 100% of every photostrip frame image (including 4x6 horizontal postcards) is displayed in full resolution without cropping.
- **August 2026 (Phase 6O - Modal Bottom Protected Security Badge Bar Removal)** ✅: Removed the bottom dark security badge bar (`COPY PROTECTED PREVIEW` / dimensions) from the modal image container in `FramesSlider.tsx` per user request, while preserving background transparent shield protection.
- **August 2026 (Phase 6P - Photostrip Frame 1.1 Custom Copywriter Description)** ✅: Updated the description (`desc`) for photostrip frame `1.1` in `FRAME_ITEMS` in `FramesSlider.tsx` to the user's custom copy: *"Style-nye elegan, item, tapi tetep berasa mewah. Aksen gold-nye bikin makin kinclong..."*
- **August 2026 (Phase 6Q - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 6R - Trusted By Partner Logo Spacing Tightening)** ✅: Tightened horizontal marquee logo spacing (`gap-1.5 sm:gap-2 md:gap-2.5`) and reduced item container dimensions (`w-16 sm:w-28 md:w-32`) in `Product.tsx` per user request ("logo-logo trusted by pepet-pepetin lagi dah"), bringing partner logos closer together for a compact, high-density brand banner presentation.
- **August 2026 (Phase 6S - Trusted By Partner Logo Size Enlargement)** ✅: Enlarged partner logo container dimensions to high-visibility scale (`h-11 sm:h-16 md:h-20` and `w-24 sm:w-36 md:w-44`) while preserving tight relative marquee gaps (`gap-2 sm:gap-2.5 md:gap-3`) in `Product.tsx` per user request ("gedein tapi logonya"), delivering a prominent, high-impact partner brand banner.
- **August 2026 (Phase 6T - Trusted By Marquee CSS Alpha Gradient Dissolve)** ✅: Removed solid white edge gradient overlay blocks and applied hardware-accelerated CSS `maskImage: linear-gradient(to right, transparent 0%, rgba(0,0,0,1) 10%, rgba(0,0,0,1) 90%, transparent 100%)` on the marquee track in `Product.tsx` per user request ("muncul ialngnya logo jangan kasi efek gradasi putih gitu dah, kasi aja efek fade trasnparan ke si logonya"), causing partner logos to smoothly fade into 100% true transparency at the screen boundaries.
- **August 2026 (Phase 6U - Multimodal 1-by-1 Visual Inspection of All 35 Photostrips)** ✅: Conducted comprehensive 1-by-1 visual inspection and structural analysis of all 35 custom photostrip frame images in `picture_stocks/photostrip-stock` (`1.1.png` to `14.2.png`) using native multimodal vision.
- **August 2026 (Phase 6V - Accurate Pop-Up Lightbox Descriptions & Features Sync)** ✅: Synchronized exact titles, categories, visual descriptions (`desc`), and specifications (`features`) across all 35 photostrip frames in `FramesSlider.tsx` and `FramesGalleryClient.tsx` based on the 1-by-1 visual inspection results, providing rich, accurate popup details in modal view previews.
- **August 2026 (Phase 6W - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 6X - FAQ Stack Slider Inactive Folder Content Hiding & Cutoff Fix)** ✅: Resolved text overlap and cutoff issue on Slide 5 (`FaqStackSlider.tsx`) where question rows from inactive background folders (e.g., Biru "TEKNIS & VENUE") peeked through the tab notch area behind the active front folder. Applied dynamic opacity toggling (`opacity-100` for active folder, `opacity-0 pointer-events-none` for inactive background folders) and expanded the top tab header spacer (`h-12 sm:h-14`), ensuring background folder tabs remain clean and flush without text leakages.
- **August 2026 (Phase 6Y - FAQ Stack Slider Folder Downward Shift & Top Clearance)** ✅: Shifted the folder stack downward on Slide 5 (`FaqStackSlider.tsx`) by increasing section top padding (`pt-20 sm:pt-24 md:pt-28 lg:pt-32`), adding folder top margin (`mt-2 sm:mt-3`), and recalibrating container heights (`h-[440px]–[550px]`) per user request ("foldernya turunin dikit"), creating optimal clearance below the floating glass header.
- **August 2026 (Phase 6Z - FAQ Stack Slider Full-Height Bottom Extension)** ✅: Extended the folder stack all the way down to the bottom of Slide 5 (`FaqStackSlider.tsx`) by removing bottom padding (`pb-0`), setting folder stack container height to full viewport flex (`h-full min-h-[460px]–[600px]`), and styling top corners with `rounded-t-[32px]/[40px]` and `rounded-b-none` per user request ("foldernya panjangin ampe bawah dah").
- **August 2026 (Phase 7A - FAQ Stack Slider Square Bottom SVG Silhouette)** ✅: Updated SVG paths (`PATH_TAB_LEFT`, `PATH_TAB_MIDDLE`, `PATH_TAB_RIGHT`) in `FaqStackSlider.tsx` to use straight rectangular bottom bounds (`L 600,460 L 0,460 Z`) instead of curved bottom corners per user request ("ini harus kotak ya biar kepotong"), eliminating white/gray corner gaps and ensuring 100% flat edge-to-edge cutoff at the bottom.
- **August 2026 (Phase 7B - FAQ Stack Slider Multi-Stop Rich SVG Gradients & Glass Sheen)** ✅: Transformed flat solid colors in `FaqStackSlider.tsx` into vibrant multi-stop linear gradients (`#FF3B5C`→`#4D0014` Coral Crimson, `#0066FF`→`#000B2B` Sapphire Navy, `#FF7700`→`#5E0F00` Electric Sunset Orange) with top metallic glass sheen overlays and glassmorphism question cards per user request ("bikin warnanya banyak gradasi di folder-foldernya biar ga flat atau borring"), delivering a high-end 3D aesthetic.
- **August 2026 (Phase 7C - Slide 3 Multi-Stop Electric Orange Gradient Background)** ✅: Transformed Slide 3 (`frames` - `FramesSlider.tsx`) background from dark navy blue to a vibrant multi-stop Electric Sunset Orange gradient (`from-[#FF7700] via-[#FF3D00] to-[#7A0F00]`) with golden amber core auras and fiery crimson ambient blur blobs per user request ("buat slide 3 warnanya oranye dan dengan gradasi biar ga flat"). Updated `bgFallbackColor` in `landingSlides.ts` to `#FF4500`.
- **August 2026 (Phase 7D - Smooth Warm Card Shadow & Edge Mask Refinement)** ✅: Replaced harsh black card box-shadows (`shadow-[0_12px_32px_rgba(0,0,0,0.7)]`) in `FramesSlider.tsx` with smooth multi-layer warm ambient shadows (`shadow-[0_22px_48px_-12px_rgba(60,10,0,0.32),0_8px_18px_-6px_rgba(0,0,0,0.18)]`) and expanded marquee edge mask bounds (`transparent 0%` to `4%` / `96%`), eliminating hard dark boundaries per user request ("bikin shadownya lebih smooth dan jangan kaya ada batasnya gitu").
- **August 2026 (Phase 7E - Edge Dark Lines Mask Removal & Ultra-Light Shadow Refinement)** ✅: Removed CSS `maskImage` from marquee track wrapper in `FramesSlider.tsx` (which previously created dirty/dark vertical blend lines across cards passing screen boundaries) and replaced heavy ambient drop shadows with an ultra-clean, soft drop-shadow (`shadow-[0_12px_28px_-6px_rgba(0,0,0,0.16)]`) per user request ("masih keliatan garisnya dan keliatan ga smooth").
- **August 2026 (Phase 7F - Gradual Edge Mask & Radial Drop-Shadow Filter)** ✅: Re-engineered Slide 3 (`FramesSlider.tsx`) marquee with a wide 14% (~200px) gradual alpha mask (`transparent 0%` to `rgba(0,0,0,1) 14%`) for ultra-smooth card fade-outs into transparency, and switched card shadows to CSS `drop-shadow-[0_16px_24px_rgba(0,0,0,0.22)]` filter to preserve rich 3D depth while completely removing rectangular bottom border lines per user request ("gw pengen sliding framenya bisa menghilang lebih smooth jadi perlahan transparan... shadownya tetep ada tapi garis bawahnya dihilangkan").
- **August 2026 (Phase 7G - High-Contrast Pure White Header Text & Drop Shadows)** ✅: Updated header title and subtitle in `FramesSlider.tsx` to 100% pure white (`text-white font-bold`) and applied strong high-contrast drop shadows (`drop-shadow-[0_4px_18px_rgba(0,0,0,0.85)]` and `drop-shadow-[0_3px_12px_rgba(0,0,0,0.8)]`) per user request ("tulisannya dibikin putih dan buat lebih terlihat kasih shadow gitu"), making text pop vividly against the bright orange background.
- **August 2026 (Phase 7H - Scroll Cue Text & Arrow Icon Size Enlargement)** ✅: Enlarged the "SCROLL UNTUK GANTI SLIDE" hint text scale (`text-xs sm:text-sm md:text-base font-extrabold tracking-[0.25em]`), enhanced amber color contrast (`text-[#F59E0B]` with `drop-shadow-[0_2px_10px_rgba(0,0,0,0.9)]`), and enlarged down arrow button (`w-10 h-10` to `w-11 h-11`) in `SlideDeckLanding.tsx` per user request ("gedein tulisannya").
- **August 2026 (Phase 7I - Marquee Padding Expansion & Shadow Cutoff Line Elimination)** ✅: Expanded vertical bottom padding on marquee container wrapper (`pb-12 sm:pb-16 md:pb-20`) and inner track (`py-4 sm:py-6 md:py-8`) in `FramesSlider.tsx` per user request ("border bawah ini yang bikin shadownya kepotong apus dah"), providing over 60px of clearance so card drop-shadows diffuse 100% naturally into transparency without getting clipped by `overflow-hidden`.
- **August 2026 (Phase 7J - Mobile Sliding Card & Frame Image Enlargement)** ✅: Enlarged sliding card width (`w-[300px] xs:w-[330px]`) and photostrip frame image display height (`h-[200px] xs:h-[225px]`) in `FramesSlider.tsx` per user request ("bagian pilih frame (slide 3) bikin lebih gedean sliding cardnya... bikin proporsional"), increasing mobile frame preview size by nearly 50% for maximum clarity and mobile screen proportion.
- **August 2026 (Phase 7K - Mobile 2-Tier Stacked Marquee Rows)** ✅: Configured mobile layout (< `sm` breakpoint) in `FramesSlider.tsx` to render **2 stacked horizontal marquee rows** moving in opposite directions (`animate-marquee-frames` top row sliding left, `animate-marquee-frames-reverse` bottom row sliding right) per user request ("pas di tampilan HP, si sliding cardnya lu bikin 2 baris aja dah"). Added `animate-marquee-frames-reverse` keyframes to `globals.css`.
- **August 2026 (Phase 7L - CSS Specificity Override Fix for Mobile 2-Row Marquee)** ✅: Fixed a CSS specificity issue where `.animate-marquee-frames` in `globals.css` forced `display: flex;`, overriding Tailwind's `hidden` class on the desktop container and causing 3 rows to appear on mobile. Removed `display: flex` from `.animate-marquee-frames` & `.animate-marquee-frames-reverse` rules so `hidden sm:flex` properly hides the desktop track on mobile screens (< `sm`), leaving **EXACTLY 2 STACKED ROWS** on mobile per user request ("inimah 3 baris kocak").
- **August 2026 (Phase 7M - Mobile 2-Row Card Dimension Enlargement)** ✅: Enlarged card width (`w-[270px] xs:w-[310px]`) and photostrip frame image display height (`h-[160px] xs:h-[190px]`) for both mobile 2-tier stacked marquee rows in `FramesSlider.tsx` per user request ("gedein lagi ukuran cardnya"), increasing preview image clarity by over 35% on mobile viewports.
- **August 2026 (Phase 7N - Section Title & Subtitle Font Size Enlargement)** ✅: Enlarged the header title font scale ("PILIH FRAME AESTHETIC-MU!") from `text-2xl–5xl` to **`text-3xl xs:text-4xl sm:text-5xl md:text-6xl lg:text-7xl`** and subtitle font scale from `text-xs–base` to **`text-sm xs:text-base sm:text-lg`** with `font-extrabold` and `drop-shadow-[0_6px_24px_rgba(0,0,0,0.9)]` in `FramesSlider.tsx` per user request ("gedein fontnya"), delivering high-impact headline readability across all screen viewports.
- **August 2026 (Phase 7O - Section Title Maximum Scale Enlargement)** ✅: Further enlarged section title scale to **`text-4xl xs:text-5xl sm:text-6xl md:text-7xl lg:text-8xl`** and subtitle scale to **`text-base xs:text-lg sm:text-xl md:text-2xl`** in `FramesSlider.tsx` per user request ("gedein lagi fontnya"), creating a massive, eye-catching headline header across mobile and desktop devices.
- **August 2026 (Phase 7P - Subtitle Thin Typography Weight Adjustment)** ✅: Changed subtitle text font weight from heavy `font-extrabold` to elegant **`font-medium text-white/95 tracking-wide`** in `FramesSlider.tsx` per user request ("bikin lebih thin tulisannya"), creating a clean, sleek typographic hierarchy below the bold main title.
- **August 2026 (Phase 7Q - Mobile Pricing Swipe Dynamic Color & Full-Card Click Trigger)** ✅: Upgraded mobile mode on Slide 4 (`PricingSlider.tsx`) per user request ("di slide 4... pas gw slide ke kanan atau ke kiri... otomatis berubah warna dan ketika di klik pada kartunya langsung masuk ke mode penjelasan lebih detail"):
  - **Dynamic Swipe Color Transition**: Background automatically transitions between Warm Amber `#F59E0B` (All You Can Photo card `activeSlide === 0`), Royal Blue `#0239A0` (Center Overview card `activeSlide === 1`), and Vibrant Teal `#0D9488` (Batch Booking card `activeSlide === 2`) on mobile swipe.
  - **Full-Card Click Trigger**: Made the entire mobile overview card container clickable everywhere, allowing users to tap any part of the card to enter the detail view immediately.
- **August 2026 (Phase 7R - Mobile Semi-Circle Side Swipe Cue Buttons)** ✅: Added fixed left and right semi-circle side buttons with bold arrow icons (`ChevronLeft` / `ChevronRight`) on mobile overview mode in `PricingSlider.tsx` per user request ("di mode hp, gw pengen di slide 4 ada semacam setengah lingkaran sama anak panah di pojok kanan dan kiri... warna setengah lingkarannya sesuai sama warna cardnya"):
  - **Dynamic Package Color Matching**: The semi-circle buttons automatically inherit the exact color gradient of the adjacent target package card (Warm Amber `#FF851B` for All You Can, Vibrant Teal `#14B8A6` for Batch Booking, Royal Blue `#0A4DBF` for Center Hero).
  - **Animated Pulse Interactivity**: Styled with `rounded-r-full` (left) and `rounded-l-full` (right), `animate-pulse`, and instant tap navigation to previous/next package card.
- **August 2026 (Phase 7S - Mobile Side Semi-Circle Placement & Container Overflow Fix)** ✅: Resolved issue where side semi-circles were clipped by container `overflow-hidden`. Placed semi-circle buttons directly on the mobile card container boundaries (`-left-7` / `-right-7` with `z-50`) and set stage container to `overflow-visible md:overflow-hidden` in `PricingSlider.tsx`, ensuring semi-circles protrude clearly on the left and right flanks of the card.
- **August 2026 (Phase 7T - Mobile Screen-Edge Flush Semi-Circle Side Buttons)** ✅: Positioned the left and right semi-circle swipe buttons 100% flush against the exact mobile screen boundaries (`absolute left-0` and `absolute right-0` with `z-40` inside full-width section container) in `PricingSlider.tsx` per user request ("bikin ini mepet sama batas kanan dan kirinya"), creating high-visibility edge navigation tabs protruding directly from the screen borders.
- **August 2026 (Phase 7U - Mobile Side Semi-Circle Vertical Offset & Overlap Resolution)** ✅: Resolved collision where the right semi-circle swipe button was hidden behind the vertical slide deck dots indicator pill (`SlideNavigationDots.tsx`) per user request ("gw pengen lu akalin soalnya ini ketutupan"):
  - **Vertical Shift**: Adjusted vertical position of both side semi-circles from `top-1/2` (50%) to **`top-[36%]`** with **`z-50`**, aligning them cleanly beside the card's upper artwork area.
  - **100% Unobstructed Visibility**: Completely clears the slide dots pill, giving both left and right semi-circle buttons crisp, unblocked touch targets on mobile HP.
- **August 2026 (Phase 7V - Mobile Side Navigation Bar Hiding & Bottom Scroll Arrow Enablement)** ✅: Configured mobile HP display per user request ("pas mode hp, gausah ada side navigation barnya deh, cukup kasih tanda panah ke bawah buat scroll lagi aja"):
  - **Side Navigation Hiding**: Set `SlideNavigationDots.tsx` to `hidden md:flex`, hiding the dark vertical slide dots pill completely on mobile screens (< `md`).
  - **Mobile Scroll Down Cue**: Enabled the animated bottom scroll down cue button ("SCROLL UNTUK GANTI SLIDE" with `ChevronDown` arrow) on mobile HP for all non-final slides in `SlideDeckLanding.tsx`, allowing mobile users to tap the down arrow to advance to the next slide.
- **August 2026 (Phase 7W - Mobile Side Semi-Circle Vertical Re-Centering)** ✅: Re-centered left and right semi-circle swipe buttons back to exact vertical center (**`top-1/2 -translate-y-1/2`**) in `PricingSlider.tsx` per user request ("buat yang slide 3 [pricing], panah sama setengah lingkarannya kasi ke tengah"), creating a balanced, centered layout on screen flanks now that side navigation dots are hidden on mobile.
- **August 2026 (Phase 7X - Mobile Semi-Circle GPU Color Wipe Transition Animation)** ✅: Implemented an animated GPU color wipe expansion on mobile HP in `PricingSlider.tsx` per user request ("kasi animasi dong pas geser di mode hp, pas misal di geser kaya warnanya nge wipe dulu ke satu halaman di mulai dari setengah lingkarannya"):
  - **Dynamic Origin Radial Expansion**: Upon swiping or tapping a side semi-circle button, a circular color wave (`motion.div` scaling from `0` to `4.5`) originates directly from the exact left (`0%, 50%`) or right (`100%, 50%`) semi-circle button.
  - **Fluid Liquid Wipe**: Sweeps the target card gradient across the screen with a hardware-accelerated cubic-bezier ease (`[0.16, 1, 0.3, 1]`) before revealing the next card.
- **August 2026 (Phase 7Y - Mobile Side Swipe GPU Wipe Z-Index Stacking Fix)** ✅: Fixed z-index stacking in `PricingSlider.tsx` per user request ("mana animasinya njir? belom muncul kocak animasinya"):
  - **High Stacking Priority (`z-30`)**: Elevated `swipeRipple` element from hidden `z-0` to **`fixed z-30 shadow-2xl`** (positioned directly over cards `z-10`/`z-20` and background layers), ensuring the expanding liquid color wipe wave is 100% visible across the entire mobile screen.
- **August 2026 (Phase 7Z - Arrow Origin GPU Radial Color Expansion)** ✅: Re-engineered origin coordinates of the liquid wipe wave in `PricingSlider.tsx` per user request ("animasi wipenya dari setengah lingkaran di samping kanan dan kiri, jadi kaya lingkaran membesar dari samping kanan dan kiri di mulai dari anak panahnya"):
  - **Exact Arrow Coordinate Anchor**: Configured fixed origin coordinates (`left: 0px` for left arrow, `left: 100vw` for right arrow, `top: 50vh`, `zIndex: 45`), causing a 250vmax radial color circle to emerge directly from inside the semi-circle arrow icon and expand 360 degrees outward across the mobile screen.
- **August 2026 (Phase 8A - Document Body Portal Mounting for Mobile Swipe Wipe)** ✅: Fixed CSS transform trap in `PricingSlider.tsx` using React `createPortal` per user request ("animasi membesarnya belum muncul bang"):
  - **Portal To `document.body`**: Rendered the expanding radial color wipe wave directly into `document.body` with `zIndex: 9999`, completely bypassing `SlideDeckLanding` CSS parent container transforms and overflow masks.
  - **100% Screen-Wide High Visibility**: Guarantees that every tap/swipe on mobile HP triggers a 250vmax liquid color circle expanding directly from the left (`0px, 50vh`) or right (`100vw, 50vh`) arrow button across the entire viewport.
- **August 2026 (Phase 8B - Target Page Color Gradient Synchronization)** ✅: Synchronized the expanding liquid wipe gradient colors to match the exact destination target slide background in `PricingSlider.tsx` per user request ("warna animasi wipe membesarnya harus sama kaya hasil akhir halaman di page selanjutnya"):
  - **Seamless Target Color Match**: Configured exact gradient classes (`bg-gradient-to-b from-[#FBBF24] via-[#F59E0B] to-[#B45309]` for All You Can, `bg-gradient-to-b from-[#14B8A6] via-[#0D9488] to-[#0F5550]` for Batch, `bg-gradient-to-b from-[#1E50D8] via-[#0239A0] to-[#001D66]` for Center Overview), ensuring 100% color continuity without any visual color jumps as the wipe completes.
- **August 2026 (Phase 8C - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 8D - Slide 4 Pricing Package Click Fix & State Variable Declaration)** ✅: Fixed bug on Slide 4 (`PricingSlider.tsx`) where clicking any package card or selection button caused no response due to undeclared state variables (`clickCoord`, `touchStartX`, and `waBase`). Declared `clickCoord` state for GPU ripple origin, `touchStartX` ref for mobile swipe gestures, and `waBase` constant for WhatsApp booking links.
- **August 2026 (Phase 8E - Mobile Slide 4 Package Detail 2x2 Grid Redesign)** ✅: Redesigned the package detail view in mobile mode on Slide 4 (`PricingSlider.tsx`). Replaced single-column vertical rows with a 2-row × 2-column grid of large square cards (`grid grid-cols-2 gap-2.5`) for both All You Can Photo and Batch Booking packages. Added 4th option ("Custom Durasi") to All You Can Photo so both categories form a 4-card 2x2 grid. Added active card ring highlights and direct WhatsApp consultation buttons.
- **August 2026 (Phase 8F - Badge Pill Removal from Pricing Cards)** ✅: Removed all red badge pills ("PALING HEMAT", "POPULER", "PALING LAKU 🔥", "VIP ULTIMATE", etc.) from both desktop and mobile pricing cards in `PricingSlider.tsx` per user request for a cleaner card layout.
- **August 2026 (Phase 8G - Mobile Card Subtitle Text Removal)** ✅: Removed redundant subtitle text (`durationOrQuota` such as "100 Lembar", "200 Lembar") from the mobile 2x2 grid cards in `PricingSlider.tsx` per user request. Enlarged package title typography (`text-lg xs:text-xl font-bayon`) for a bolder, cleaner presentation.
- **August 2026 (Phase 8H - Premium Mobile 2x2 Grid Cards Design Polish)** ✅: Upgraded mobile 2x2 grid cards in `PricingSlider.tsx` with high-end aesthetic design: (1) Circular icon badges (`Zap`, `Sparkles`, `Users`, `PartyPopper`) in pastel containers; (2) Vibrant status pill tags (`PAS & HEMAT`, `REKOMENDASI 🔥`, `EVENT AKBAR 👑`, `MOST POPULAR 🔥`); (3) Soft multi-layer gradient backdrops with glowing 3D shadows; (4) Bold typography scale for package names; (5) Shimmering theme-matched gradient CTA buttons.
- **August 2026 (Phase 8I - Mobile Selected Card Clipping Fix)** ✅: Fixed issue where selected cards in mobile 2x2 grid (`PricingSlider.tsx`) got clipped at container edges. Added padding (`p-2 pt-1 pb-4`) to `mob-detail-mode` scroll wrapper, `p-1` to grid container, and replaced outer `ring-4` + `scale-[1.02]` with inset `border-4` theme border (`border-teal-400` / `border-amber-400`), guaranteeing 100% sharp and unclipped card boundaries.
- **August 2026 (Phase 8J - Slide 5 Mobile FAQ Folder Layout Calibration)** ✅: Calibrated mobile display scale in `FaqStackSlider.tsx`: (1) Scaled top tab titles (`text-[11px] xs:text-xs`) to prevent tab header text clipping; (2) Switched row alignment from `justify-evenly` to `justify-start` with compact spacing (`space-y-2 xs:space-y-2.5`); (3) Added bottom padding (`pb-16 xs:pb-20`), ensuring the last question card ("Download Softfile") sits well above screen bottom without colliding with the floating WhatsApp CTA button.
- **August 2026 (Phase 8K - Slide 5 Mobile Typography & Card Scale Upgrade)** ✅: Upgraded mobile typography and layout scale in `FaqStackSlider.tsx`: (1) Enlarged top tab header text (`text-[12.5px] xs:text-[14px] font-bayon`); (2) Increased question titles (`text-[15px] xs:text-[16.5px] font-black`), subtitles (`text-[11.5px] xs:text-[12.5px]`), and metric highlights (`text-[13px] xs:text-[14.5px]`); (3) Enlarged icon badges (`w-9.5 h-9.5 xs:w-10.5 h-10.5`); (4) Expanded card padding (`p-3 xs:p-3.5`) and vertical spacing (`space-y-3 xs:space-y-3.5`).
- **August 2026 (Phase 8L - Full Mobile FAQ Folder Height Coverage & 5th FAQ Item)** ✅: Eliminated empty bottom space in `FaqStackSlider.tsx` mobile display: (1) Added 5th FAQ question item to each folder (`Custom Backdrop`, `Garansi & Backup`, `Reschedule Tanggal`); (2) Changed flex layout to `justify-between` with full height distribution, stretching 5 chunky cards evenly from top to bottom of the folder body; (3) Preserved WhatsApp button clearance.
- **August 2026 (Phase 8M - Mobile FAQ Folder Height Compression)** ✅: Compressed folder height on mobile HP in `FaqStackSlider.tsx`: (1) Set mobile height to compact ratio (`h-[58vh] xs:h-[62vh]`), ending well above bottom screen edge; (2) Center-aligned folder block (`justify-center pb-8`); (3) Replaced stretched `justify-between` with compact `justify-start` spacing (`space-y-1.5 xs:space-y-2`), eliminating empty dead space.
- **August 2026 (Phase 8N - Mobile FAQ Folder Bottom Flush Positioning)** ✅: Positioned mobile FAQ folder block flush against screen bottom in `FaqStackSlider.tsx`: (1) Restored `justify-end pb-0` flex alignment; (2) Applied `rounded-b-none` so folder sits 100% flat and flush on screen bottom without floating.
- **August 2026 (Phase 8O - Slide 5 Header Top Clearance Adjustment)** ✅: Increased top padding in `FaqStackSlider.tsx` (`pt-20 xs:pt-22 sm:pt-24`), shifting the "Frequently Ask Question" title down to eliminate visual overlap with the floating glass navigation bar.
- **August 2026 (Phase 8P - FAQ Header Generous Top Clearance)** ✅: Increased top padding in `FaqStackSlider.tsx` (`pt-26 xs:pt-28 sm:pt-30`), giving generous top clearance below the navbar glass header and backdrop shadow.
- **August 2026 (Phase 8Q - FAQ Folder Tab & Card Typography Scale Boost)** ✅: Enlarged tab header typography (`text-[15.5px] xs:text-[17.5px] font-bayon`) and question card titles (`text-[16.5px] xs:text-[18px] font-black`), subtitles (`text-[12.5px] xs:text-[13.5px]`), and metric highlights (`text-[14px] xs:text-[15.5px]`) in `FaqStackSlider.tsx` for extra-large, prominent readability on mobile.
- **August 2026 (Phase 8R - FAQ Header True Vertical Centering)** ✅: Centered the FAQ title and subtitle block vertically inside the top zone (`flex-1 flex-col justify-center items-center`) between the floating glass header bar and the top of the folder stack in `FaqStackSlider.tsx`.
- **August 2026 (Phase 8S - FAQ Tab Text True 3D Z-Index Layering)** ✅: Bound tab header text directly inside each folder's `<motion.div>` element in `FaqStackSlider.tsx`. Removed global floating `z-50` overlay, so each folder's SVG path and its tab title share the exact same z-index layer (`z-40` when active, `z-10/20/30` when inactive). Folder 2's orange SVG shape now naturally clips over Folder 1's inactive text with 100% realistic 3D depth.
- **August 2026 (Phase 8T - FAQ Tab Typography Scale & Lettering Space Enhancement)** ✅: Enlarged tab header typography (`text-[16px] xs:text-[18px] font-bayon`) and applied custom letter spacing (`tracking-[0.05em] sm:tracking-wider`) in `FaqStackSlider.tsx` for prominent, distinct, high-impact tab branding.
- **August 2026 (Phase 8U - FAQ Main Section Title Scale Boost)** ✅: Enlarged main section title "Frequently Ask Question" to `text-3xl xs:text-4xl sm:text-5xl md:text-6xl lg:text-[64px]` and subtitle to `text-xs xs:text-sm sm:text-base md:text-lg` in `FaqStackSlider.tsx` for bold, high-visibility visual impact.
- **August 2026 (Phase 8V - Extreme Mobile FAQ Title Scaling)** ✅: Scaled FAQ title "Frequently Ask Question" dynamically on mobile view (`text-[10vw] xs:text-[11vw] tracking-tighter whitespace-nowrap`) in `FaqStackSlider.tsx` so the title spans almost 100% of the mobile screen width with massive, high-impact typography.
- **August 2026 (Phase 8W - 3-Line Mobile FAQ Title Stack with Letter Spacing)** ✅: Reformatted "FREQUENTLY ASK QUESTION" into a 3-line editorial stack on mobile (`FREQUENTLY` / `ASK` / `QUESTION`) with giant font scale (`text-4xl xs:text-[44px]`) and generous letter spacing (`tracking-[0.06em]`) in `FaqStackSlider.tsx`.
- **August 2026 (Phase 8X - Massive 3-Line Mobile FAQ Title Upgrade)** ✅: Upgraded font size of the 3-line FAQ title stack to giant scale (`text-[52px] xs:text-[58px] sm:text-6xl md:text-7xl`) in `FaqStackSlider.tsx` for massive, high-impact editorial typography.
- **August 2026 (Phase 8Y - Desktop & Laptop FAQ Header Visibility Fix)** ✅: Fixed bug where the FAQ header title was hidden on desktop/laptop screens in `FaqStackSlider.tsx`. Set `shrink-0` on header block and controlled folder stage height (`sm:h-[460px] md:h-[500px] lg:h-[540px]`), reserving guaranteed top clearance so "FREQUENTLY ASK QUESTION" is 100% visible across all desktop and laptop monitors.
- **August 2026 (Phase 8Z - Perfect FAQ Vector Curve Corner Rendering)** ✅: Removed CSS box-radius clipping (`rounded-t-[28px]` and `overflow-hidden`) from outer `<motion.div>` in `FaqStackSlider.tsx`. This allows SVG Bezier curves (`PATH_TAB_LEFT`, `PATH_TAB_MIDDLE`, `PATH_TAB_RIGHT`) to draw 100% pure, smooth, pixel-perfect folder tab silhouettes and shadows with zero box-radius clipping artifacts.
- **August 2026 (Phase 8AA - Pinterest-Style Portfolio & Gallery Slide Deck Integration)** ✅: Created new Slide 04 `PortfolioSlider.tsx` featuring a Pinterest Masonry collage grid layout for Sebooth photo booth results and photostrips: (1) Pinterest-style multi-column masonry grid (`columns-2 sm:columns-3 lg:columns-4`) with varying aspect ratios (`aspect-[2/3]`, `aspect-[4/5]`, `aspect-[3/4]`); (2) Filter categories (`SEMUA PORTO`, `PHOTOSTRIP 2x6`, `POSTCARD 4R`, `WEDDING VIBES`, `BIRTHDAY & EVENT`); (3) Interactive Lightbox modal preview with full HD photostrip details & direct WhatsApp request CTA; (4) Integrated seamlessly into `landingSlides.ts` & `SlideItem.tsx`.
- **August 2026 (Phase 8AB - Single-Row Horizontal Scrollable Gallery Reel Upgrade)** ✅: Updated `PortfolioSlider.tsx` to a single-row horizontal scrollable carousel reel format per user instruction ("satu baris banyak foto yang ditampilkan dan bisa di scroll"): (1) Single-row horizontal scroll track (`overflow-x-auto snap-x snap-mandatory flex gap-4 sm:gap-6`); (2) Left & Right navigation buttons (`ChevronLeft` & `ChevronRight`) + touch swipe/drag support; (3) 10+ high-res Photostrip (2x6) & Postcard (4R) cards; (4) Interactive Lightbox modal preview with WhatsApp request CTA.
- **August 2026 (Phase 8AC - Portfolio Section Archive Badge Removal)** ✅: Removed the `✨ REAL PHOTOBOOTH GALLERY ARCHIVE` badge pill from the top header in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request for a cleaner and more direct headline presentation.
- **August 2026 (Phase 8AD - Portfolio Category Filter Pills Removal)** ✅: Removed the category filter buttons row (`SEMUA PORTO`, `PHOTOSTRIP (2x6)`, `POSTCARD (4R)`, `WEDDING VIBES`, `BIRTHDAY & EVENT`) from the header in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request, maximizing vertical space for the single-row horizontal photo reel.
- **August 2026 (Phase 8AE - Pinterest Adaptive Masonry Wall Layout Upgrade)** ✅: Transformed [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) into an authentic Pinterest Masonry wall with adaptive photo sizing and staggered composition per user request ("foto foto dalam susunan dan size yang adaptif untuk setiap fotonya buat ditampilin kaya pinterest"):
  - **Adaptive Multi-Column Masonry**: Responsive CSS column layout (`columns-2 sm:columns-3 md:columns-4 xl:columns-5 gap-2.5–4`) where cards render at their natural individual aspect ratios (2x6 photostrips `aspect-[1/2.6]`, 4R postcards `aspect-[4/3]`, candid moments `aspect-[3/4]` & `aspect-[4/5]`, and square cuts `aspect-[1/1]`).
  - **Pinterest Interactions**: Red/White Pinterest-style "SIMPAN" button on hover, interactive heart like counter, top category pill tags, and zoom preview indicator.
  - **Smooth Scroll Wall**: Seamless internal vertical scroll with thin scrollbar allowing visitors to explore 16+ diverse photobooth moments and frame styles.
  - **Interactive Lightbox Modal**: High-definition preview modal with specs, tags, and direct WhatsApp request CTA.
- **August 2026 (Phase 8AF - Portfolio Section Subtitle Text Removal)** ✅: Removed the subtitle text ("Koleksi hasil cetak photostrip, postcard 4R, dan keseruan event Sebooth dengan gaya Pinterest") from the header in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request for an ultra-clean, distraction-free headline.
- **August 2026 (Phase 8AG - Portfolio Section Main Title Header Removal)** ✅: Removed the main title header (`PORTOFOLIO HASIL FOTO & PHOTOSTRIP`) from [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request, creating a 100% full-screen immersive Pinterest photo masonry wall.
- **August 2026 (Phase 8AH - 100% Original Uncropped Aspect Ratio Preservation)** ✅: Eliminated all forced `aspect-[...]` container ratios and `object-cover` styling in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("aespect ratio dari fotonya itu original sesuai sama fotonya, jangan lu kecilin atau ubah2"). Images now render at their 100% natural, uncropped, unscaled original dimensions (`w-full h-auto object-contain`) in both the Pinterest masonry grid and lightbox preview.
- **August 2026 (Phase 8AI - Card Title & Subtitle Overlay Removal)** ✅: Removed the bulky title and subtitle text overlays from each pin card in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("gausa kasi judul sama subtitle"), keeping 100% of each photostrip and photo artwork completely unblocked and visually clean.
- **August 2026 (Phase 8AJ - Card Category Badge Pill Removal)** ✅: Removed the top-left category badge pill (`PHOTOSTRIP 2X6`, `POSTCARD 4R`, etc.) from each pin card in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("apus juga ini"), leaving each card in a minimalist, 100% artwork-first presentation.
- **August 2026 (Phase 8AK - Full-Width Edge-to-Edge Photo Wall Layout & Multi-Column Density)** ✅: Expanded the Pinterest gallery wall in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) to fit 100% of the screen width (`w-full`) per user request ("pas in aja ama lebar layarnya buat area nampilin si foto-fortonya"):
  - Removed `max-w-7xl` container constraints, allowing photos to span edge-to-edge across the entire screen.
  - Expanded column density up to 7 columns (`columns-2 sm:columns-3 md:columns-4 lg:columns-5 xl:columns-6 2xl:columns-7`).
  - Added 24+ high-resolution photostrip frames & event moments to completely eliminate empty bottom/side areas on wide displays.
- **August 2026 (Phase 8AL - Nested Scroll Wheel & Touch Gesture Routing Fix)** ✅: Resolved scroll mechanism bug on Slide 4 (`PortfolioSlider.tsx`) where mouse wheel and touch gestures on the gallery were hijacked by the slide deck controller per user request ("mekanisme scroll di page gallery masih belom jalan"):
  - Built `findScrollableParent` detector in [SlideDeckLanding.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/slides/SlideDeckLanding.tsx) to differentiate between slide transitions and internal container scrolling.
  - When scrolling inside the Pinterest gallery, mouse wheel smoothly scrolls the photo wall until reaching boundary limits (top/bottom) before allowing slide transitions.
  - Enabled native touch scrolling (`touch-pan-y`) on mobile devices.
- **August 2026 (Phase 8AM - Gallery Deep Performance Optimization & 60fps Scroll Acceleration)** ✅: Resolved severe gallery lag and memory footprint in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) and [SlideDeckLanding.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/slides/SlideDeckLanding.tsx) per user request ("bikin lebih optimal, ini lemot dan berat banget di bagian gallery"):
  - **next/image WebP/AVIF Compression**: Migrated all raw `<img>` tags to Next.js `Image` with automatic WebP conversion and `quality={65}`, shrinking asset load from ~120MB uncompressed bitmaps to ~1.2MB (~99% RAM and bandwidth reduction).
  - **Native Browser Compositor Scroll**: Optimized wheel event routing in `SlideDeckLanding.tsx` to let the browser execute native 60fps/120fps GPU compositor scrolling without JS layout reflow locks.
  - **CSS `content-visibility: auto`**: Added `content-visibility: auto; contain-intrinsic-size: 280px` to cards, skipping paint/render calculations for offscreen elements.
  - **GPU Fillrate Optimization**: Removed heavy full-screen blur divs (`blur-[160px]`) and simplified card layers.
- **August 2026 (Phase 8AN - Scrollbar Visibility Hiding & Scroll Hint Removal)** ✅: Completely removed visible scrollbars and bottom scroll guide text in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("apus panduan scroll"):
  - Applied `[scrollbar-width:none] [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden` to hide the native browser scrollbar across Chrome, Edge, Safari, and Firefox.
  - Removed bottom guide text ("↓ SCROLL KE BAWAH UNTUK MELIHAT SEMUA PORTOFOLIO SEBOOTH ↓").
- **August 2026 (Phase 8AO - Clean White Background Theme for Gallery Wall)** ✅: Changed Slide 4 (`portfolio`) background from dark mode to clean pure white (`bg-[#FFFFFF]`) in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) and [landingSlides.ts](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/config/landingSlides.ts) per user request ("backgroundnya bikin warna putih aja"):
  - Styled pin cards with light-theme borders (`border-slate-200/90`), subtle depth shadows (`shadow-[0_4px_16px_rgba(0,0,0,0.06)]`), and hover highlights.
  - High-contrast Pinterest Save button and bottom like/zoom pill overlay.
- **August 2026 (Phase 8AP - Dynamic Visual Rhythm & Staggered Aspect Ratio Interleaving)** ✅: Implemented dynamic Pinterest collage rhythm in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("gw pengen meskipun aespect ratio atau sizenya sama, tapi gw pengen tampilannya agak variatif, jadi ukurannya keliatan beragam tapi masih pas"):
  - Structured alternating mathematical interleaving across Tall 2x6 strips (1:3), Wide 4R postcards (3:2 / 4:3), Square candids (1:1), and Medium portraits (4:5 / 3:4).
  - Ensured every column across responsive viewports has an organic, non-repeating vertical rhythm where no two adjacent cards share identical heights.
  - Added subtle tactile card framing variants (`polaroid`, `elevated`, `standard`) for rich Pinterest texture.
  - Added `pb-20` safe clearance for comfortable mobile scrolling.
- **August 2026 (Phase 8AQ - Multi-Scale Layout Architecture with Full-Width Hero Breakouts)** ✅: Implemented multi-scale visual layout in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("bikin tampilan layoutnya dibikin beragam, kaya ada yang gede banget (lebarnya samain kaya lebar layar hp) atau ya cukup lebar dan kecil"):
  - Added `hero-full` breakout cards with `[column-span:all]` that stretch across the entire width of the mobile phone screen and desktop gallery view with rich spotlight showcase styling.
  - Interspersed with `featured-wide` cards, `polaroid` framed cards, `compact` cards, and `standard` photostrips to provide extreme scale contrast and visual delight.
- **August 2026 (Phase 8AR - Elimination of Card Text Detail Panels / Pure Artwork Presentation)** ✅: Removed all text detail panels (titles, subtitle, tags, badges, buttons) from large `hero-full` cards in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("gausa pake detail gitu, foto doang"):
  - All cards across all scale variants (giant full-width, wide, polaroid, compact, standard) now render purely as 100% visible, unblocked artwork photos with sleek hover-only Pinterest interactions.
- **August 2026 (Phase 8AS - Organic High-Contrast Size Collage with 7 Distinct Formats)** ✅: Upgraded [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) to a completely organic, non-uniform Pinterest moodboard collage per user request ("kek menurut gw masi terlalu rapi polanya, bikin agak variatif dong jadi kaya macem-macem size fotonya"):
  - Introduced 7 distinct aspect ratio scale classes across 28 items: `hero-full` (100% full screen span), `extra-tall` (1:3.2 long vertical strips), `tall` (1:2.6 strips), `wide` (3:2 / 4:3 4R postcards), `square` (1:1 candids), `portrait` (4:5 / 3:4 moments), and `mini` (compact cuts).
  - Shuffled layout order with organic vertical margin variations (`mb-2` to `mb-6`) and varied corner radii (`rounded-xl`, `rounded-2xl`, `rounded-3xl`), eliminating predictable patterns.
- **August 2026 (Phase 8AT - Authentic Native Pinterest App Feed Layout Replica)** ✅: Transformed [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) into a 1:1 replica of the native Pinterest mobile feed app layout per user screenshot ("nih bikin kaya gini layoutnya"):
  - Built a 2-column masonry grid on mobile (`columns-2 gap-3.5`) and multi-column on desktop (`sm:columns-3 md:columns-4 lg:columns-5 xl:columns-6 gap-3 sm:gap-4`).
  - Rendered all cards as smooth full-bleed rounded images (`rounded-2xl overflow-hidden shadow-sm hover:shadow-md`) with natural aspect ratios (3:4, 1:2.2, 1:1, 1:2.8, 4:3, etc.).
  - Added native Pinterest pin footer under each image with short title on the left and `•••` action button on the right.
  - Retained Pinterest Red `#E60023` "SIMPAN" button and hover/touch interaction overlays.
- **August 2026 (Phase 8AU - Multi-Line Full Text Display / Removal of Ellipsis Truncation)** ✅: Removed all `truncate` text truncation across accordion FAQ rows in [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx) and pin footers in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) per user request ("tulisannya kalo ga cukup tampilin aja, sampe beberapa baris aman ko"):
  - Replaced `truncate` with `break-words leading-tight flex-1` so question titles and subtitles ("Coverage Luar Kota", "Waktu Reservasi", "Reschedule Tanggal", etc.) wrap naturally and completely across multiple lines without being cut off with `...`.
- **August 2026 (Phase 8AV - Dedicated Gallery Media Asset Policy Established)** 📌: Registered user guideline per directive ("nanti gw kasi file-file fotonya buat ditampilin di gallery, ntar lu bisa pake itu. gw gapengen lu pake file foto yang sama buat ditampilin di gallery padahal bukan buat gallery"):
  - Strict boundary established: The Pinterest Gallery ([PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx)) will exclusively consume dedicated gallery photo assets provided by the user, and will NEVER cross-contaminate or reuse assets intended for other sections (e.g. frame catalog templates, hero backgrounds, etc.).
- **August 2026 (Phase 8AW - FAQ Header Vertical Realignment & Snug Folder Proximity)** ✅: Realigned the FAQ header in [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx) per user request ("turunin lagi, pepetin ama section dropdown foldernya"):
  - Brought the "FREQUENTLY ASK QUESTION" header and subtitle down to sit snugly right on top of the folder tab dropdowns (`justify-end items-center`), eliminating large empty middle gaps and maximizing vertical folder height.
- **August 2026 (Phase 8AX - FAQ Header Typography Expansion to Screen Edges)** ✅: Scaled up the "FREQUENTLY ASK QUESTION" title typography in [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx) to `text-[54px] xs:text-[62px] sm:text-5xl md:text-6xl lg:text-[76px] xl:text-[84px]` per user request ("gedein lagi ampe lebar tulisannya aga mepet sama halaman layar HP"):
  - Expanded the typography so the bold Bayon text spans tightly across the mobile viewport width while remaining crisp and impactful.
- **August 2026 (Phase 8AY - FAQ Header Line Spacing Calibration)** ✅: Adjusted the vertical inter-line spacing and line-height of the FAQ header in [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx) per user request ("jarakin dikit antar barisnya"):
  - Relaxed the line-height from tight `leading-[0.82]` to clean `leading-[0.96] xs:leading-[0.98]` with a healthy flex gap (`gap-1 xs:gap-1.5`) between stacked words and subtitle margin (`mt-1.5 xs:mt-2`).
- **August 2026 (Phase 8AZ - Comprehensive Codebase Cleansing & Dead Code Removal)** 🧹: Performed exhaustive removal of deprecated files and legacy inline visual editing systems per user directive ("cleansing semua file coding yang uda ga kepake di tampilan landing page sekarang, kedepannya gw mau update foto-fotonya aja lewat admin page"):
  - **Deleted 11 Unused Legacy Sections**: `About.tsx`, `FAQ.tsx`, `FeaturedFrames.tsx`, `Gallery.tsx`, `Hero.tsx`, `InstagramFeed.tsx`, `Location.tsx`, `News.tsx`, `Pricing.tsx`, `ProcessPinningSection.tsx`, `Testimonials.tsx`.
  - **Deleted 14 Deprecated Visual Editor Components**: `AdminEditProvider.tsx`, `ClientProviders.tsx`, `EditModeToggle.tsx`, `EditableArrayItemText.tsx`, `EditableImage.tsx`, `EditableText.tsx`, `EditableTextAdvanced.tsx`, `GalleryMediaEditor.tsx`, `IframeEditBridge.tsx`, `ImageCropModal.tsx`, `LayoutEditorModal.tsx`, `PreviewEditOverlay.tsx`, `SectionVisibilityControl.tsx`, `TextEditModal.tsx`.
  - **Decoupled Client Providers**: Cleaned `layout.tsx`, `Header.tsx`, `Footer.tsx`, `AboutContent.tsx`, `PartnershipContent.tsx`, `Product.tsx`, `FramesSlider.tsx` to render pure native React components without edit hooks.
- **August 2026 (Phase 8BA - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 8BB - Mobile Animation Performance & 60/120 FPS Optimization)** ⚡: Overhauled mobile animation performance across all slides and components to eliminate stuttering and frame drops on handheld devices while strictly preserving all rich animations:
  - **Eliminated Backdrop-Blur GPU Bottlenecks**: Replaced expensive `backdrop-blur-md` filters on continuous 2-tier marquee cards ([FramesSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FramesSlider.tsx)), 3D coverflow card action buttons ([Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx)), and question accordion rows ([FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx)) with crisp, high-contrast semi-transparent solid RGBA styling, eliminating thousands of real-time Gaussian blur passes per second.
  - **GPU Hardware-Compositing Acceleration**: Upgraded `.animate-pan-left`, `.animate-marquee-left`, `.animate-marquee-frames`, and `.animate-marquee-frames-reverse` in [globals.css](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/globals.css) with `transform: translate3d(0, 0, 0)`, `backface-visibility: hidden`, `contain: layout paint`, and calibrated mobile durations.
  - **Ambient Blur GPU Fillrate Relief**: Made giant 500px–700px ambient blur background blobs desktop-only (`hidden sm:block`) in [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx).
  - **Swipe Ripple Optimization**: Streamlined mobile swipe ripple wipe in [PricingSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PricingSlider.tsx) with GPU layer isolation and removed heavy 80px blur box-shadows.
  - **Slide Deck Layer Isolation**: Added `[transform:translate3d(0,0,0)]` and `[backface-visibility:hidden]` in [SlideDeckLanding.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/slides/SlideDeckLanding.tsx) for 120fps mobile native compositor scrolling.
- **August 2026 (Phase 8BC - FAQ Title Resizing & Folder Tag Width Fitting)** ✅: Refined the FAQ section ([FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx)) per user request:
  - **Downscaled Section Title**: Reduced "FREQUENTLY ASK QUESTION" to clean, balanced editorial typography (`text-2xl xs:text-3xl sm:text-4xl md:text-5xl lg:text-[54px]`) and simplified subtitle margin/sizing so the header looks proportional and not overly huge.
  - **Proportional Folder Tag Fitting**: Re-engineered SVG vector Bezier tab curves (`PATH_TAB_LEFT`, `PATH_TAB_MIDDLE`, `PATH_TAB_RIGHT`) with symmetrical 140px peak spans and equal 33.3% flex tab button widths (`w-1/3`), perfectly fitting each label ("FRAME & PROPS", "TEKNIS & VENUE", "UMUM & BOOKING") without text truncation or awkward edge overflow across mobile and desktop viewports.
- **August 2026 (Phase 8BD - FAQ Dropdown Direct Questions & High-Performance CSS Grid Animation)** ⚡: Overhauled the FAQ accordion system in [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx) per user request:
  - **Direct Question & Answer Layout**: Stripped all redundant subtitles, subheadings, and badge highlights. Rephrased all 15 FAQ items across all 3 folders into direct, natural questions that reveal concise answers immediately upon clicking.
  - **Ultra-Fast CSS Grid Dropdown Animation**: Replaced heavy JavaScript height-recalculation loops with native GPU compositor CSS Grid row transitions (`grid-template-rows: 0fr -> 1fr`), eliminating all accordion expansion lag and delivering instantaneous response on mobile and desktop devices.
- **August 2026 (Phase 8BE - Mobile Side Nav Bar Glassmorphism & Instant Animation Overhaul)** ✨: Redesigned the mobile side navigation drawer in [Header.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/layout/Header.tsx) per user request:
  - **Minimalist Glassmorphism UI (No Icons / No Subtitles)**: Removed all icon boxes, icon images, and secondary subtitles from every menu option. Replaced with clean, elegant glassmorphic pills (`bg-white/5` -> `bg-white/20`, `border-white/10` -> `border-white/35`) featuring wide Bayon typography and vibrant orange active dot indicators.
  - **Ultra-Fast Slide-Out Transition**: Eliminated heavy spring physics and double-blur backdrop stalling. Upgraded to hardware-composited GPU cubic-bezier slide transitions (`duration: 0.22`, `ease: [0.22, 1, 0.36, 1]`, `transform: translate3d(0,0,0)`, `will-change: transform`) for buttery-smooth 120 FPS drawer open/close on mobile devices.
- **August 2026 (Phase 8BF - Product & Frame Popup Modal Hardware-Compositing Overhaul)** ⚡: Eliminated stuttering and frame drops when clicking product cards and photostrip frames ([Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx), [FramesSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FramesSlider.tsx), [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx)) per user request:
  - **Eliminated Full-Screen Backdrop Blur Bottlenecks**: Replaced expensive `backdrop-blur-2xl` and `backdrop-blur-xl` on 100vw x 100vh modal overlays with crisp, opaque `bg-black/85` and `bg-black/90`, saving millions of GPU pixel shading calculations per frame.
- **August 2026 (Phase 8BG - Mobile Popup Modal Compact Resizing & Badge Removal)** 📱: Scaled and decluttered modal popups in [Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx) and [FramesSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FramesSlider.tsx) per user request:
  - **Removed Photo Overlay Badges**: Removed the "MOST FAVORITE" / category badge pill overlaid on top of the product photo inside the modal popup view for a clean, unobstructed view of the equipment and frame graphics.
- **August 2026 (Phase 8BH - Popup Modal Image Enlargement & High-Impact Visuals)** 🖼️: Enlarged the preview photo containers inside the modal popup views in [Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx) and [FramesSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FramesSlider.tsx) per user request:
  - **Product Popup Image Scaling**: Increased product image container height to `h-56 xs:h-64 sm:h-72 md:h-auto min-h-[220px] xs:min-h-[250px] md:min-h-[340px]` with centered cover scaling for a large, vivid look at the physical photobooth hardware.
  - **Frame Template Popup Scaling**: Expanded frame preview height to `h-[280px] xs:h-[330px] sm:h-[420px] md:h-[480px]` with clean object-contain rendering, making template graphics and layout details crisp, prominent, and easy to inspect.
- **August 2026 (Phase 8BI - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 8BJ - Admin Session Lookup & Claim Inspector)** 🔍: Built complete session claim lookup feature in the Admin panel ([SessionLookupTab.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/admin/SessionLookupTab.tsx), `/api/admin/session-lookup` route) allowing admins to inspect who claimed any specific photobooth session:
  - **Single Session Search & Quick Recent List**: Admin can input any Session ID (UUID) or click from the 10 most recent sessions list with instant claim status indicators.
  - **User & Claimer Metadata**: Displays customer Full Name (`full_name`), Email, Phone/WhatsApp (`phone_number`), direct WhatsApp chat link, User UUID, and claim timestamp.
  - **Zero Heavy Image Loading (Bandwidth & Vercel Limit Saver)**: Specifically designed to avoid loading heavy image tags or uncompressed assets. Displays lightweight media count badges (photos, GIFs, videos, total media) to strictly conserve Vercel image optimization and data quotas.
  - **Queue Ticket Association**: Shows linked queue ticket number, queue registrant name, and event info if the session was generated via the digital queue system.
- **August 2026 (Phase 8BK - GitHub Repository Synchronization)** 🚀: Staged, committed, and pushed all recent feature updates (Session Lookup API, SessionLookupTab component, Admin Panel tab integration, and `.gitignore` refinements) to GitHub `main` branch.
- **August 2026 (Phase 8BL - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 8BM - AI Context Read & Sync)** ✅: Read and verified `AGENTS.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **August 2026 (Phase 8BN - Local Static Image Optimization Fail-Safe Fix)** 🖼️: Fixed issue where product card photos, partner logos, pricing cards, and frame previews failed to load with broken image icons on local development and client machines. Root cause: Next.js 16 image optimizer (`/_next/image`) threw 500 errors or failed on local PNG assets without sharp. Solution: Added `unoptimized` prop to all local static `<Image>` components across `Product.tsx`, `PricingSlider.tsx`, `FramesSlider.tsx`, and `PortfolioSlider.tsx`, allowing the browser to fetch static PNGs directly from `/public` instantly with zero optimization errors and zero bandwidth quota penalties.
- **August 2026 (Phase 8BO - GitHub Repository Synchronization & Deployment Fix)** 🚀: Staged, committed, and pushed all recent image loading optimizations (`unoptimized` fail-safe fixes across Product, Frames, Pricing, and Portfolio sections) and documentation updates to GitHub `main` branch for automatic Vercel production deployment.
- **September 2026 (Phase 8BP - AI Context Read & Sync)** ✅: Read and verified `agents.md` file upon user request ("baca agents.md") to align AI context with project guidelines, directory structure, tech stack, database schema, design system, and latest roadmap updates.
- **September 2026 (Phase 8BQ - Pinterest Gallery Real Photo Population & Random Selection Pipeline)** 📸: Populated Slide 4 Pinterest Gallery with real photos from `picture_stocks/gallery-sebooth/gallery`: (1) Created automated node extraction script (`processGalleryPhotos.mjs`) scanning 43 session directories; (2) Selected exactly 1 photostrip (`strip_*.jpg`) and 1 randomly chosen candid/pose photo (`photo_*.jpg`) per session for a total of 86 high-res photos; (3) Copied and structured assets into `public/images/gallery/` with native image dimension extraction (JPEG SOF / PNG IHDR binary parsing); (4) Generated type-safe dataset `src/data/galleryPins.ts` with interleaved photostrip and candid pose rhythm; (5) Connected [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx) to consume `GALLERY_PINS` with zero bandwidth quota penalty.
- **September 2026 (Phase 8BR - Gallery Bottom CTA Relocation & Column Decoupling)** ✅: Moved "LANJUT KE PRICING & PAKET" CTA card outside the multi-column CSS grid in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx), placing it at the absolute bottom of the entire 86-photo Pinterest feed so it never appears prematurely in intermediate columns or overlaps side navigation dots.
- **September 2026 (Phase 8BS - Product Section Title Navbar Clearance Fix)** ✅: Resolved header clipping bug on Slide 2 in [Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx): Increased section top padding (`pt-[76px] xs:pt-[82px] sm:pt-24 md:pt-28`), updated font line-height to `leading-normal` with top clearance, and balanced spatial coverflow heights so "SEBUTIN APA YANG LOE MAU!" is 100% visible below the floating glass header.
- **September 2026 (Phase 8BT - Copywriting Tone Standardization / Polite & Engaging Modern Indonesian)** ✨: Replaced all informal slang words ("Loe", "Lu", "Gue", "Gua", "gw", "ampe", "kagak") across all slides and components ([Product.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/Product.tsx), [PricingSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PricingSlider.tsx), [FaqStackSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/FaqStackSlider.tsx), [landingSlides.ts](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/config/landingSlides.ts)) with polite, friendly, and modern copy (e.g. "Kamu", "Anda", "Kami", "Untuk acara kamu", "Sebutin Apa Yang Kamu Mau!").
- **September 2026 (Phase 8BU - Pinterest Masonry Height-Balanced Multi-Column Engine / Zero Gap Fix)** 🧱: Replaced fragile CSS multi-column (`columns-2`) with an authentic greedy height-balanced flex column engine in [PortfolioSlider.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/sections/PortfolioSlider.tsx): Calculates real-time aspect ratio accumulators across dynamic responsive columns (`2` mobile to `6` desktop), always feeding incoming pins to whichever column currently has the shortest height. This completely eliminates empty white voids, uneven column bottoms, and broken layout gaps.
- **September 2026 (Phase 8BV - Header Navbar Order Synchronization & Partnership Removal)** 🧭: Synchronized navbar items across mobile drawer and desktop floating header in [Header.tsx](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/components/layout/Header.tsx): (1) Removed the `PARTNERSHIP` link; (2) Reordered links to match exact chronological slide deck order: `PRODUCT` -> `FRAMES` -> `GALLERY` -> `PRICING` -> `FAQ` -> `MY PHOTOS`; (3) Connected desktop item clicks to smooth in-page hash scroll handler with alias fallbacks (`product`/`services` and `faq`/`contact`).
- **September 2026 (Phase 8BW - My Photos UI Instagram-Style Carousel Overhaul)** 📱: Overhauled `src/app/profile/page.tsx` ([MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx)) according to user reference: (1) Maximized photo real estate with full-bleed rounded-3xl dark cards occupying 68vh; (2) Integrated horizontal touch gestures and swipe navigation for multi-media carousels (Strips, Candid Studio Poses, HD Softfiles); (3) Added floating top glass user badge, media indicator dots, and bottom overlay HUD (Event Title, Date & Time, Heart Like counter, Attendees avatar bubbles, Share, Download, and Bookmark buttons); (4) Built story-style horizontal session selector with `ALL` button and circle avatar badges; (5) Implemented modern mobile bottom navigation bar (`Explore`, `My Photos`, `Book`, `Frames`, `Admin`) and fullscreen HD lightbox modal.
- **September 2026 (Phase 8BX - Session Detail Page UI Modernization & Alignment)** 🎨: Modernized `src/app/profile/[sessionId]/page.tsx` ([SessionDetailPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/%5BsessionId%5D/page.tsx)) from brutalist borders to high-end glassmorphism cards matching the Instagram carousel aesthetic: (1) Rounded-3xl media container with sleek backdrop blur; (2) Segmented pill switcher (`Strip`, `GIF`, `Live Video`, `Photos`); (3) Instant HD Download & Share actions; (4) Eliminated brutalist hard-shadows and updated background to `#F7F9FC`.
- **September 2026 (Phase 8BY - Pixel-Perfect My Photos Mobile Frame Overhaul)** 🎯: Re-engineered `src/app/profile/page.tsx` to match the exact proportions and layout of the user reference image: (1) Wrapped in a high-end mobile frame shell (`max-w-[420px]`); (2) Top header with large sans-bold `Events` title, filter button, and add button; (3) Main card with stacked card shadow peek, verified user avatar badge, and bottom overlay actions (`[Heart + Like count + (+99) Attendees]`, Share, Bookmark, Price `$120`); (4) Clean single-row story bubble selector with `ALL` button and numbered badges; (5) Bottom app navigation bar (`Explore`, `Community`, `Add trip`, `Favourites`, `Account`).
- **September 2026 (Phase 8BZ - Dual-Axis Swipe Gesture Engine (Vertical Session Switch / Horizontal Photo Switch))** 🔄: Implemented the exact requested gesture controls on [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) **Vertical Swipe (Up/Down)**: Swaps between different photo sessions (`activeSessionIndex`) with fluid Y-axis deck transitions; (2) **Horizontal Swipe (Left/Right)**: Navigates through all photos/strips/candid poses within the current active session (`currentMediaIndex`) with animated pagination dots (`• • •`); (3) Synchronized story avatar selection with vertical direction tracking.
- **September 2026 (Phase 8CA - 3 Real Multi-Photo Sessions Integration for Testing)** 📸: Extracted 3 complete real sessions from `picture_stocks/gallery-sebooth/gallery/` into `public/images/sessions/` and created `src/data/demoSessions.ts`: (1) **Session 1 (Måneskin in Paris)**: 1 Photostrip + 4 Studio Candid Poses + 1 Live GIF (6 photos); (2) **Session 2 (Sarah & Kevin Wedding Party)**: 1 Wedding Strip + 4 Studio Candid Poses + 1 Live GIF (6 photos); (3) **Session 3 (Tokyo Vibes Shibuya Street)**: 1 Custom Neon Strip + 4 Studio Candid Poses + 1 Live GIF (6 photos); (4) Connected directly to [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) with full vertical swipe & horizontal swipe functionality.
- **September 2026 (Phase 8CB - Zero-Delay Pre-Rendered Dual-Axis Carousel Engine / 120 FPS Native Feel)** ⚡: Eliminated stutter and delay caused by unmounting/remounting cycles in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) Pre-renders all sessions and all media items into hardware-accelerated CSS `translate3d` tracks; (2) Next photos and next sessions are already present in DOM memory side-by-side and top-to-bottom ("muncul dari awal"); (3) Added native touch & mouse drag listener for instantaneous 120 FPS swipe feedback with keyboard arrow support (`ArrowUp`/`ArrowDown`/`ArrowLeft`/`ArrowRight`).
- **September 2026 (Phase 8CC - Multi-Source HD Download Route Stream Engine)** 💾: Enhanced [route.ts](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/api/download/route.ts) to natively handle both local public assets (`/images/sessions/`, `/images/gallery/`) and external CDN / Supabase storage URLs with automatic MIME detection (`.jpg`, `.png`, `.gif`, `.webp`, `.mp4`) and attachment disposition headers for instant one-click HD downloads.
- **September 2026 (Phase 8CD - 1:1 Live Tactile Drag Engine & WebP Memory Optimization)** 🚀: Resolved sluggishness and high memory pressure on [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) Installed Sharp and compressed all raw ~2.7MB session photos down to 85KB WebP assets (95% memory & VRAM reduction); (2) Implemented live 1:1 tactile drag tracking (`dragOffset` with zero transition lag during finger movement); (3) Dynamic axis locking (`dragAxis`) separating vertical session slides from horizontal photo slides; (4) Added spring release snap curve `cubic-bezier(0.25, 1, 0.5, 1)` for buttery smooth 120 FPS native responsiveness.
- **September 2026 (Phase 8CE - Full Photo Real Estate & Text Overlay Removal)** 🧹: Removed the session title text, date/subtitle string, and top watermark overlay from [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request ("tulisannnya apus aja"): (1) Gives 100% clean, unobstructed focus to the photostrip and candid portraits; (2) Retains only the sleek floating action pills at the bottom (`[Heart Likes + Attendees]`, Share, Bookmark, and Price tag).
- **September 2026 (Phase 8CF - Bottom Action Pills Removal & Pure Full-Bleed Photo Focus)** 🖼️: Removed the entire bottom action pill row (`[Heart + Likes + Attendees]`, Share, Bookmark, and Price tag `$120`) from [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user screenshot request: (1) Bottom gradient overlay removed for 100% natural photo brightness and clarity; (2) Maximum photo canvas area from top to bottom; (3) Preserved instant tap-to-expand lightbox with HD download action.
- **September 2026 (Phase 8CG - 100% Uncropped Full Frame Fit & Ambient Blur Backdrop)** 📐: Updated photostrip and photo media containers in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) to `object-contain` with expanded vertical card height (`h-[66vh] xs:h-[68vh] sm:h-[70vh]`): (1) Eliminates all horizontal & vertical image cropping so 100% of photostrips, logos, text headers, and graphics are fully visible; (2) Added matching ambient blurred glow backdrop behind the photo for seamless luxury aesthetics.
- **September 2026 (Phase 8CH - Top User Badge Removal & Edge-to-Edge Photo Presentation)** 🧹: Removed the top-left floating avatar, username (`tom.dilan12`), and location subtitle badge from [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request: (1) Preserved sleek top-right pagination indicator dots (`• • •`) and options trigger; (2) Leaves the card entirely clean for maximum uncropped visual immersion.
- **September 2026 (Phase 8CI - Pagination Dots Relocation to Bottom Center)** 🔘: Moved the Instagram-style horizontal media indicator pill dots (`[ • • • • • • ]`) to the bottom-center of the card in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user screenshot request: (1) Centered floating glass pill with dynamic active dot width; (2) Keeps the top bar minimal with only the subtle right-aligned options menu.
- **September 2026 (Phase 8CJ - Dedicated Full-Width Photo & Media Thumbnail Selector Bar)** 📸: Replaced the bottom generic navigation bar (`Explore`, `Community`, `Add trip`, `Favourites`, `Account`) on [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) with a full-width photo thumbnail selector: (1) Shows direct instant-tap thumbnails for every photo (`Strip`, `Photo 1`, `Photo 2`, `Photo 3`, `Photo 4`, `GIF`) of the active session; (2) Includes active session indicator switcher (`SESI 1/3`); (3) Synchronized bidirectional touch swipe and instant thumbnail tap selection.
- **September 2026 (Phase 8CK - Per-Session Dedicated Bottom Thumbnail Selector)** 🗂️: Converted the bottom thumbnail bar in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) to represent photo sessions (*Sesi 1, Sesi 2, Sesi 3...*): (1) Each thumbnail displays the session cover with its session number badge (`1`, `2`, `3`); (2) Tapping any session thumbnail jumps directly to that session; (3) Active session highlights with orange ring and smooth scaling.
- **September 2026 (Phase 8CL - Instagram 3D Photo Stack & Tap Shuffle Animation Engine)** 🎴: Re-architected photo display inside each session in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) to match the official Instagram Stories Polaroid / Photo Stack sticker aesthetic: (1) **Layered 3D Depth**: Front photo (`scale 1, 0deg`), 1st card peeking behind (`rotate 5.5deg, scale 0.96`), and 2nd card peeking behind (`rotate -6deg, scale 0.91`) with rounded squircle corners (`rounded-[38px]`) and soft depth shadows; (2) **Tap to Shuffle**: Tapping the card fires an organic spring shuffle animation (`cubic-bezier(0.34, 1.4, 0.64, 1)`) cycling the front photo to the back and bringing the next card forward; (3) **Multi-Gesture Support**: Touch swipe left/right, touch swipe up/down, and instant tap all seamlessly integrated.
- **September 2026 (Phase 8CM - Swipe to Switch Sessions & Tap to Cycle Photo Stack)** 🔄: Configured full gesture mapping on [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request: (1) **Swipe Right / Swipe Left (and Swipe Up / Down)**: Fluidly transitions between photo sessions with 1:1 live drag tracking; (2) **Single Tap / Spacebar**: Triggers the Instagram 3D Photo Stack shuffle animation to cycle through all photos in the current session.
- **September 2026 (Phase 8CN - Tap Debounce Guard & Diffuse Ambient Shadow Polish)** 🎴: Resolved tap skipping and refined photo deck shadows on [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) Added strict 280ms tap debounce (`lastTapTime`) blocking simulated double-event triggers from touch+mouse, guaranteeing exactly 1 photo advance per tap; (2) Removed hard outline borders (`border-white/10`) and artificial bottom shadow strips; (3) Replaced with natural multi-layer diffuse drop shadow (`shadow-[0_22px_50px_-10px_rgba(0,0,0,0.45),0_10px_20px_-5px_rgba(0,0,0,0.25)]`) seamlessly spreading into the background with zero hard edges.
- **September 2026 (Phase 8CO - Horizontal Side-by-Side Session Slider Mechanism)** ↔️: Re-architected session deck transition container to pure horizontal side-by-side positioning in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) Sesi lain ditempatkan di sebelah kanan & kiri dalam track `flex flex-row overflow-hidden`; (2) Swipe ke kanan atau ke kiri meluncurkan transisi sesi horizontal secara instan tanpa ada kebocoran vertikal di bawah; (3) Tapping foto di sesi aktif tetap menjalankan animasi Instagram 3D Stack shuffle.
- **September 2026 (Phase 8CP - Subtle 3D Depth Polish & Natural Diffuse Ambient Shadow)** 🎨: Refined the card deck rotation angles and shadow rendering in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) Softened tilt angles from ±6° to subtle ±3° with controlled opacity (82% and 60%), removing bulky dark corner blocks; (2) Replaced harsh shadow with an ultra-soft dual-layer ambient shadow (`shadow-[0_20px_45px_-12px_rgba(15,23,42,0.28),0_8px_20px_-6px_rgba(15,23,42,0.16)]`); (3) Cards now blend seamlessly and naturally into the background.
- **September 2026 (Phase 8CQ - Deep Multi-Layer Diffuse Shadow & Unboxed Layout)** 🌑: Upgraded shadow dynamics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request: (1) Applied deep multi-layer diffused shadow (`shadow-[0_28px_65px_-12px_rgba(0,0,0,0.48),0_14px_30px_-8px_rgba(0,0,0,0.28),0_0_20px_rgba(0,0,0,0.1)]`); (2) Removed outer bounding-box corner clipping and added padding (`py-2 px-3`) so the deep shadow spreads out 65px+ smoothly into the backdrop without any hard box outlines.
- **September 2026 (Phase 8CR - Elimination of Protruding Background Outline Lines)** 💎: Completely removed all protruding background corner lines and dark box wedges in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) Inactive deck layers (`diff > 0`) cleanly set to `opacity: 0` until brought forward; (2) The active photo card renders as a single pristine squircle frame with uncropped photo presentation and rich ambient drop shadow; (3) 100% clean edges with zero dark outline artifacts.
- **September 2026 (Phase 8CS - Pure Direct Photo Drop-Shadow & Container Box Elimination)** 🌟: Eliminated the entire dark capsule container background (`bg-slate-950`) enclosing the photo in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx): (1) The container is now 100% transparent; (2) The shadow (`shadow-[0_25px_60px_-15px_rgba(0,0,0,0.45),0_12px_28px_-8px_rgba(0,0,0,0.25)]`) is attached directly to the rounded photo element itself; (3) Resolves all bounding border lines, providing a pure, floating, natural photostrip presentation.
- **September 2026 (Phase 8CT - Organic Scattered Instagram Instant Photo Pile)** 🎴: Configured organic physical photo pile aesthetics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request: (1) Foto-foto di belakangnya tampil acak bertumpuk dengan sudut rotasi natural (Kartu 1 di `+5.5°`, Kartu 2 di `-6.5°`, Kartu 3 di `+4°`) memperlihatkan foto-foto asli di baliknya; (2) Tiap foto memiliki sudut melengkung halus `rounded-[26px]` dan bayangan drop-shadow natural tanpa kotak pembungkus hitam; (3) Tap animasi spring shuffle `cubic-bezier(0.34, 1.4, 0.64, 1)` mengocok tumpukan dengan mulus seperti Instagram Instant sticker.
- **September 2026 (Phase 8CU - Instagram Stories Sticker Photo Stack & Orbital Reaction Dock Redesign)** 🎯: Rebuilt [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) to replicate the exact UI model from user reference image (Instagram Story sticker cutout & reaction dock):
  - **AMOLED Pitch Black Theme**: Pure black background (`#000000`) with sleek minimal mobile canvas (`max-w-[420px]`).
  - **Top Navigation Bar**: Left close `✕` icon, center bold title `"Instagram lately:"`, and right action buttons: 4-square grid icon (`00 00` / `LayoutGrid`) to open the session picker sheet, plus camera icon (`Camera`) to open session claim / scanner.
  - **Center Organic Squircle / Pebble Photo Stack**: Photo cards clipped to organic pebble contours (`border-radius: 46% 54% 58% 42% / 48% 46% 54% 52%`) with background photos peeking organically from top-right (`translate3d(18px, -18px, 0) rotate(5deg) scale(0.96)`) and top-left; tapping front photo triggers smooth spring shuffle animation to cycle through photos in current session.
  - **Account Attribution Row**: Centered directly under the photo cutout featuring a circular avatar badge (`SEB`), author handle (`sebooth.id`), timestamp (`2h`), and `•••` options trigger modal (HD view, download, share).
  - **Orbital Emoji Reaction Dock**: 5 circular dark buttons arranged in a smile/constellation arc: Peak top-center `❤️` (Heart like), top-left `😍` (Heart-eyes), top-right `+` (Claim/Add session), bottom-left `🤩` (Starry-eyed), and bottom-right `📸` (Flash camera/HD softfiles). Tapping emojis fires floating animated reaction particle bursts shooting upwards across the screen (`.animate-emoji-burst`).
- **September 2026 (Phase 8CW - Bottom Dock Removal, Photo Scale Maximization & My Photos Title Header)** 🚀: Updated [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request & screenshot references:
  - **Removed Bottom Elements**: Completely deleted the attribution info bar (`tom.dilan12 · 2h · •••`) and the entire 5-button orbital emoji reaction dock (`❤️`, `😍`, `+`, `🤩`, `📸`) for an ultra-clean, distraction-free gallery view.
  - **Maximized Photo Canvas Dimensions**: Scaled the photo stack container to fill almost the entire screen height (`h-[74vh]–[80vh] max-h-[640px]`, `w-[300px]–[390px]`), providing a large, prominent view of all photostrips and candid portraits.
  - **Renamed Header Title to "My Photos"**: Updated top title from `"Instagram lately:"` to **`"My Photos"`** in bold dark typography (`text-slate-900 font-extrabold`).
  - **Integrated Card Controls**: Added floating top-right actions (`Maximize2` HD Lightbox & `MoreHorizontal` session options) and bottom pagination dots directly over the active photo card.
- **September 2026 (Phase 8CX - Natural Aspect Ratio Adaptation & Spacious PC Multi-Session Deck Layout)** 📐: Overhauled [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **100% Original Uncropped Aspect Ratio Preservation**: Card container dynamically morphs its aspect ratio based on the active media type: tall 2x6 photostrips render in natural uncropped vertical strip proportions (`aspect-[1/2.8] max-w-[330px]`), while candid studio poses render in classic portrait proportions (`aspect-[3/4] max-w-[470px]`), preventing header or graphic cropping.
  - **Spacious Multi-Session PC Layout**: Expanded carousel canvas to `max-w-6xl` on desktop with wide padding (`px-4 sm:px-12 md:px-24 lg:px-36`). Side sessions now sit at a comfortable, generous distance with subtle stage lighting (`opacity-25 md:opacity-35 scale-90 blur-[0.5px]`), completely eliminating the tight crowding on PC monitors.
  - **Desktop Flank Navigation Arrows**: Added floating circular navigation buttons (`ChevronLeft` and `ChevronRight`) on the left and right screen flanks for mouse and keyboard browsing on PC.
- **September 2026 (Phase 8CY - Exact 1200x1800px (2:3 Aspect Ratio) Master Photo Standard)** 🖼️: Calibrated photo card container in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) to match the master print frame resolution **1200×1800 px (`aspect-[2/3]`)**:
  - Sets the exact 2:3 ratio across all session cards (`aspect-[2/3] max-h-[580px]–[680px] w-auto max-w-[400px]–[500px]`), displaying full photostrip pairs, postcards, poses, Undip 1957 artwork, and QR codes with 100% pixel-perfect accuracy and zero side-crop distortion.
- **September 2026 (Phase 8CZ - Initial Mobile Swipe Gesture Guide Overlay)** 👆: Integrated interactive swipe guide overlay for mobile screens in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) using `picture_stocks/swipe.png`:
  - **Asset Integration**: Transferred `picture_stocks/swipe.png` to `/public/images/swipe.png` for fast zero-latency static rendering.
  - **Animated Floating Hand Badge**: Added `@keyframes swipe-guide-hand` in `globals.css` with horizontal sliding and trail animation (`.animate-swipe-hand`) inside a frosted glass badge with pulsating ping glow.
  - **Mobile-Only Presentation (`md:hidden`)**: Displays over the active 1200x1800 card on initial load with clear instructions: *"Swipe kiri / kanan untuk ganti sesi, atau tap foto untuk melihat pose lainnya"*.
  - **Smart Dismissal**: Automatically fades out after 5 seconds, upon the user's first touch/drag (`handleStart`), or upon tapping "Mengerti" / overlay background.
- **September 2026 (Phase 8DA - Dedicated HD Download Action Integration)** 📥: Added prominent HD download action triggers in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx):
  - **Photo Card Top-Right Quick Action**: Added emerald download button directly on the active 1200x1800 photo card next to the fullscreen lightbox and session options buttons.
- **September 2026 (Phase 8DB - Direct Card Download Click Propagation & Header Clean Up)** 🎯: Refined download mechanics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Single Corner Placement**: Removed redundant download button from top header, concentrating all photo actions directly in the top-right corner of the active photo card.
  - **Fixed Tap Bubble Event Conflict**: Added strict `onMouseDown={(e) => e.stopPropagation()}` and `onTouchStart={(e) => e.stopPropagation()}` handlers to card buttons (`Download`, `Maximize2`, `MoreHorizontal`), completely stopping parent drag/tap shuffle logic from triggering next photo.
  - **Direct Anchor File Download**: `handleDownload` constructs and programmatically clicks an anchor element linking directly to `/api/download` with attachment headers for instant file download without page jumps.
- **September 2026 (Phase 8DC - Removal of Bottom Card Indicator Pill)** 🧹: Removed the bottom dark frosted pagination pill (`• • • • •`) from [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request, leaving the entire bottom artwork, strip frames, and QR codes 100% clean and unobstructed.
- **September 2026 (Phase 8DD - Outline Border Elimination & Continuous Shadow Padding)** 🖼️: Polished card edge aesthetics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request & cropped screenshot:
  - **Removed Card Outline**: Stripped away the `border border-slate-200/90` bounding box around the photo cards for a pure borderless presentation.
  - **Zero Shadow Clipping**: Set gesture and card stage containers to `overflow-visible` with calibrated height (`max-h-[560px]–[660px]`), allowing ambient drop-shadows to bleed and disperse naturally without sharp bottom horizontal boundary line cuts.
- **September 2026 (Phase 8DE - Seamless Circular Ring Carousel Loop)** 🔄: Upgraded swipe transitions and gesture mechanics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) to a circular carousel loop.
- **September 2026 (Phase 8DF - Clean Linear Sliding Experience)** 📐: Reverted session carousel mechanics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) from circular looping back to a crisp, natural linear slider per user preference:
  - **Natural Linear Horizontal Track**: Sessions sit side-by-side on an expansive horizontal track with smooth cubic-bezier sliding transforms.
  - **Bounded Navigation Limits**: Gestures, flank buttons, and arrow keys respect slide boundaries (stopping smoothly at the first and last sessions). Flank buttons smoothly disable with reduced opacity when at ends.
  - **Clean Zero-Rotation Presentation**: Active photo renders at pure upright orientation with unrotated cards behind for a crisp, high-end gallery feel.
- **September 2026 (Phase 8DG - Interactive Zoom-Out Multi-Session Overview Matrix)**: Built an in-place zoom-out bird's-eye overview mode in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Fluid Zoom-Out Keyframe Animations**: Added .animate-zoom-out-overview and .animate-zoom-in-focus in [globals.css](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/globals.css) with silky cubic-bezier(0.16, 1, 0.3, 1) scaling transitions.
  - **4-Grid Header & Title Trigger**: Clicking the 4-square grid button in the top header or clicking the session header title toggles between Focus View (single card track) and Zoom-Out Overview (all sessions grid).
  - **Multi-Card Matrix Deck**: Displays all sessions in a 2-to-4 column responsive grid with uncropped 2:3 photo cards, active session highlight ring, photo counter badges, and session titles.
  - **Instant Zoom-In on Tap**: Tapping any session card instantly focuses on it and smoothly zooms back into the full-card centerpiece view. Esc key also cleanly dismisses overview mode.
- **September 2026 (Phase 8DH - In-Place Optical Zoom-Out Deck & Total Text Clutter Removal)**: Refined session overview mechanics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **In-Place Centered Optical Zoom-Out**: Built a single unified continuous track. When clicking the 4-grid toggle, the active session card scales down directly from its center position (	ranslate3d math precisely locks the active session to 50% center), smoothly revealing adjacent sessions sliding in on the left and right.
  - **Total Text Deletion**: Removed all text details underneath cards (event titles, subtitles, dates) and banner notices for a 100% pure, borderless, high-end gallery aesthetic.
  - **Tap-to-Zoom-In Focus**: Tapping any card in zoomed-out mode glides it to the center and scales it up smoothly to 1:1 focus view.
- **September 2026 (Phase 8DI - Compact Mini-Cards & Justified Centered Alignment in Zoom-Out)**: Upgraded zoom-out overview mode in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Compact Mini Photostrips (Kecil-Kecil)**: Scaled zoom-out card height to compact proportions (max-h-[220px]–[370px]), making all sessions fit comfortably on screen at a glance.
  - **Justified Centered Layout (Justify Alignment)**: In Zoom-Out mode, the entire slider track adopts justify-center gap-3 sm:gap-6 with 	ranslate3d(0,0,0), distributing all session cards symmetrically in the center of the viewport.
  - **Hidden Flank Arrows**: Desktop flank arrows smoothly hide in zoom-out mode to prevent card overlap, and reappear instantly upon zoom-in.
- **September 2026 (Phase 8DJ - Precision Active-Session Centered Zoom-Out Anchor)**: Upgraded zoom-out transition anchoring in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Selected Session Anchor Center**: Calculated the exact transform coordinate calc(50% - ( * )) ensuring the active session card never shifts sideways during zoom-out. It physically shrinks in place at 50% center of the screen, while other sessions glide in symmetrically on either side.
  - **Clean Mini Aspect Preservation**: Mini photo cards scale to max-h-[220px]–[370px] with balanced slide padding and no text clutter.
- **September 2026 (Phase 8DK - Spacious Justified Multi-Card Spacing in Zoom-Out)**: Fixed card cramping in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Generous Justified Alignment**: Switched overview mode to a spacious flex layout with justify-center gap-6 sm:gap-10 md:gap-16 lg:gap-20, giving all session cards clean breathing room across the full width of the screen.
  - **Proportional Card Sizing**: Mini cards scale naturally (max-h-[260px]–[410px] aspect-[2/3]) without clipping or bumping into neighbors.
- **September 2026 (Phase 8DL - Unified Anchor Coordinate Zoom-Out Transition)**: Polished zoom-out optical mechanics in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Selected Session Anchor Lock**: The active session card remains strictly locked to the center coordinate (50%) while shrinking down from full-size into its mini card state, ensuring the zoom-out animation starts directly and seamlessly from the selected photo.
  - **Spacious Natural Gaps**: Configured min(48vw, 300px) slide slots with px-3 sm:px-6 padding, giving all mini cards generous breathing room so they never touch or cramp each other.
- **September 2026 (Phase 8DM - Absolute Justified Centering & Uniform Cards in Zoom-Out)**: Perfected zoom-out alignment in [MyPhotosPage](file:///c:/Users/AXIOO%20HYPE%20R5/Documents/2026/06%20Sebooth%20Proposal%20Company%20Profile/sebooth-website/src/app/profile/page.tsx) per user request:
  - **Absolute Centered Distribution (Justify Absolut)**: In Zoom-Out mode, the entire collection of session cards immediately distributes and aligns in the absolute horizontal center of the screen (justify-center gap-6 sm:gap-10 md:gap-16 lg:gap-20 with 	ranslate3d(0, 0, 0)).
  - **Uniform Pure Cards (Tanpa Pilihan Sesi Khusus)**: Removed the orange selection border ring in zoom-out mode. All session cards render with identical clean borders, full vibrant opacity, and rich soft shadows.
  - **Instant Tap Zoom-In**: Tapping any card in this clean justified overview immediately zooms in to that card as the full-screen centerpiece.

